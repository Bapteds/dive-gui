# PLAN.md — DIVE Turbinen · openFOAM Solver Control

> Plan d'implémentation de la base applicative.
> Objectif de cette phase : **fondations** — auth, back office admin de gestion des comptes, page d'accueil vierge.
> Le pilotage réel d'`openFoamSolver` (serveur distant) **n'est PAS** dans le périmètre de cette phase (pas de serveur dispo). On prépare seulement le terrain.

---

## 1. Décisions validées (avec l'utilisateur)

| Sujet | Choix | Note |
|-------|-------|------|
| API | **Vrai backend Node + TypeScript** (Express) | Serveur séparé, REST, prêt à brancher openFOAM plus tard. |
| Persistance | **SQLite (fichier local) via Prisma** | Zéro install serveur ; migration Postgres triviale ensuite. |
| Authentification | **JWT access + refresh** | Access token court (15 min), refresh en cookie `httpOnly` (7 j). |
| Rôles | **`super-admin` / `user`** | `super-admin` indélébile et non rétrogradable. |

> Choix du framework backend : **Express** (léger, explicite, TypeScript). Alternative écartée pour l'instant : NestJS (plus structuré mais lourd pour une base). À rediscuter si le périmètre grossit.

---

## 2. Architecture du dépôt (monorepo)

```
app/
├── apps/
│   ├── api/                 # Backend Express + TS + Prisma
│   │   ├── prisma/
│   │   │   ├── schema.prisma
│   │   │   └── seed.ts       # crée le super-admin initial
│   │   ├── src/
│   │   │   ├── config/       # env, constantes
│   │   │   ├── lib/          # prisma client, jwt, hashing, logger
│   │   │   ├── middleware/   # auth, role guard, error handler, validation
│   │   │   ├── modules/
│   │   │   │   ├── auth/      # routes login / refresh / logout / me
│   │   │   │   └── users/     # CRUD comptes (back office)
│   │   │   ├── app.ts        # app Express
│   │   │   └── server.ts     # bootstrap
│   │   ├── .env.example
│   │   └── package.json
│   └── web/                  # Frontend React + Vite + TS
│       ├── src/
│       │   ├── app/          # routes, providers, guards
│       │   ├── components/   # UI réutilisable + primitives shadcn
│       │   ├── features/
│       │   │   ├── auth/     # login, contexte session
│       │   │   └── admin/    # back office users
│       │   ├── lib/          # client API (fetch + refresh auto), utils
│       │   ├── pages/        # Home, Login, Admin
│       │   ├── styles/
│       │   │   └── tokens.css # design tokens (source unique CSS vars)
│       │   └── main.tsx
│       ├── tailwind.config.ts # miroir des tokens
│       └── package.json
├── package.json              # workspaces (npm) + scripts racine
├── AGENTS.md                 # (existant) règles front
├── CLAUDE.md                 # (existant) charte design
└── PLAN.md                   # ce fichier
```

> Découplage strict front/back. Le front parle au back uniquement via le client API (`apps/web/src/lib/api`). Quand le serveur openFOAM existera, on ajoutera un module `solver/` côté API sans toucher au reste.

---

## 3. Modèle de données (Prisma / SQLite)

```prisma
model User {
  id           String   @id @default(cuid())
  email        String   @unique
  fullName     String
  passwordHash String
  role         Role     @default(USER)
  isProtected  Boolean  @default(false) // true = super-admin indélébile
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}

enum Role {
  SUPER_ADMIN
  USER
}
```

- **Mots de passe** : hash `argon2` (jamais en clair, jamais loggués).
- **Seed** : crée 1 `super-admin` (`isProtected = true`) depuis variables d'env (`SEED_ADMIN_EMAIL`, `SEED_ADMIN_PASSWORD`). Idempotent.

---

## 4. API REST — contrat

Préfixe : `/api/v1`. Réponses JSON. Erreurs normalisées `{ error: { code, message } }`.

### Auth
| Méthode | Route | Accès | Rôle |
|---------|-------|-------|------|
| POST | `/auth/login` | public | — | corps `{ email, password }` → access token + pose le cookie refresh |
| POST | `/auth/refresh` | cookie | — | renvoie un nouvel access token |
| POST | `/auth/logout` | auth | — | invalide le cookie refresh |
| GET  | `/auth/me` | auth | — | profil de l'utilisateur courant |

### Users (back office)
| Méthode | Route | Rôle requis |
|---------|-------|-------------|
| GET    | `/users` | `super-admin` | liste paginée |
| POST   | `/users` | `super-admin` | crée un compte |
| GET    | `/users/:id` | `super-admin` | détail |
| PATCH  | `/users/:id` | `super-admin` | modifie (nom, rôle, mot de passe) |
| DELETE | `/users/:id` | `super-admin` | supprime |

**Règles métier dures (testées) :**
- Un compte `isProtected` (super-admin) **ne peut pas** être supprimé → `409`.
- Le super-admin ne peut pas être rétrogradé en `user`.
- On ne peut pas supprimer son **propre** compte.
- Email unique ; validation des entrées via `zod`.

---

## 5. Sécurité

- Hash mots de passe : `argon2id`.
- JWT signés (`jsonwebtoken`), secret en env ; access 15 min, refresh 7 j (cookie `httpOnly`, `SameSite=Lax`, `Secure` en prod).
- Middleware : `requireAuth` (vérifie l'access token) + `requireRole('SUPER_ADMIN')`.
- `helmet`, CORS restreint à l'origine front, rate-limit sur `/auth/login`.
- Aucune donnée sensible en log. Variables secrètes uniquement via `.env` (jamais commit ; `.env.example` fourni).

---

## 6. Frontend — écrans & routing

Stack imposée par `CLAUDE.md` / `AGENTS.md` : React + Vite + TS, Tailwind branché sur les tokens, shadcn/ui, `lucide-react`.

- **Routing** : `react-router`. Garde de route (`<RequireAuth>`, `<RequireRole>`).
- **Session** : contexte React + client API qui rafraîchit l'access token automatiquement sur `401`.
- **Écrans** :
  1. `/login` — formulaire email + mot de passe (états : idle, loading, erreur).
  2. `/` — **page d'accueil vierge** (layout + header de marque, contenu vide volontairement).
  3. `/admin` — back office (réservé `super-admin`) : table des comptes, création, édition, suppression avec confirmation. Le super-admin apparaît avec un badge « protégé » et actions delete/downgrade désactivées.
- **États gérés partout** : loading, empty, error, hover, focus, disabled (DoD CLAUDE.md).
- **Design** : 100 % via tokens (`tokens.css` + `tailwind.config`), palette stricte `#004A99 / #EE7F00 / #BCBDBF` + échelle. Thème clair uniquement.

> ⚠️ Règle CLAUDE.md §0 : avant TOUT JSX/CSS, exécuter la séquence skills **1) ui-ux-pro-max → 2) frontend-design → 3) design-taste-frontend → 4) web-design-guidelines**. Aucune ligne d'UI ne sera écrite avant ça.

---

## 7. Qualité / outillage

- TypeScript strict des deux côtés. ESLint + Prettier. Code **commenté en anglais**.
- Tests : `vitest` (front) + tests API (supertest) ciblés sur les règles métier critiques (suppression super-admin, auth, rôles).
- Scripts racine : `dev` (front + api en parallèle), `build`, `lint`, `test`, `db:migrate`, `db:seed`.
- `full-output-enforcement` : aucun placeholder ni `// TODO` tronqué dans le code livré.

---

## 8. Découpage en lots (ordre d'exécution proposé)

1. **Lot 0 — Scaffolding** : monorepo (workspaces), configs TS/ESLint/Prettier, `.env.example`, scripts racine.
2. **Lot 1 — Backend auth & DB** : Prisma + SQLite, schéma, seed super-admin, modules auth (login/refresh/logout/me), middlewares sécurité. + tests.
3. **Lot 2 — Backend users** : CRUD + règles métier (protection super-admin). + tests.
4. **Lot 3 — Front fondation** : Vite, tokens design, Tailwind, shadcn, client API, contexte session, routing + gardes. *(skills 1→4 d'abord)*
5. **Lot 4 — Écrans** : Login, Home vierge, Back office Admin. *(skills 1→4 d'abord)*
6. **Lot 5 — Finitions** : responsive, a11y AA, review `web-design-guidelines`, README de lancement.

> Je te propose de **valider ce plan**, puis d'exécuter lot par lot (je te montre le résultat à chaque lot). Dis-moi si tu veux ajuster le framework backend, le périmètre des rôles, ou l'ordre des lots.

---

## 9. Points encore ouverts (à confirmer si besoin)

- **Gestionnaire de paquets** : npm workspaces par défaut
- **Inscription publique** : aucune — les comptes sont créés uniquement par le super-admin via le back office.
- **Réinitialisation mot de passe / email** : hors périmètre de cette base (pas de serveur mail). À planifier plus tard.

---

## 10. Journal des modifications de code
<!-- Chaque modification de code est notée ici (exigence CLAUDE.md §0). Format : date — lot — fichiers — description. -->

| Date | Lot | Fichiers / zone | Description |
|------|-----|-----------------|-------------|
| 2026-06-19 | Design (skills 1→3) | `PRODUCT.md`, `DESIGN.md` | Direction design issue de `ui-ux-pro-max` → `impeccable` (substitut de `frontend-design`, non installé) → `design-taste-frontend`. Contrat visuel + tokens verrouillés + specs des 3 écrans. |
| 2026-06-19 | Lot 0 (Scaffolding) | racine, `apps/api`, `apps/web` | Monorepo npm workspaces ; `@dive/api` (Express+TS CommonJS, app bootable, env validé zod) + `@dive/web` (Vite+React 18+TS, alias `@`) ; configs TS/ESLint9/Prettier ; install unique (555 paquets, argon2 natif OK). Gates verts (typecheck/build/lint/health). |
| 2026-06-19 | Lot 1+2 (Backend) | `apps/api/prisma`, `apps/api/src`, `apps/api/tests` | Prisma/SQLite (modèle `User` ; `role` en `String` car SQLite/Prisma 5.22 sans enum natif, enum appliqué côté zod) + migration `init` + seed super-admin argon2id idempotent. JWT access+refresh, révocation par `tokenVersion` au logout, cookie refresh httpOnly path-scopé. Middlewares auth/role/validate/rate-limit. CRUD `/users` + règles dures (`PROTECTED_ACCOUNT`, `PROTECTED_ROLE`, `SELF_DELETE_FORBIDDEN`, `EMAIL_TAKEN`). 29 tests supertest verts. Note : `DATABASE_URL=file:./dev.db` (résolu relativement à `prisma/`). |
| 2026-06-19 | Lot 3 (Front fondation) | `apps/web/src` | `tokens.css` + `tailwind.config.ts` (charte exacte, tout en variables CSS), primitives shadcn/Radix tokenisées (button/input/field/password/badge/table/dialog/alert-dialog/select/dropdown/tooltip/avatar/separator/skeleton/sonner), client API (refresh single-flight sur 401), contexte session (token en mémoire + bootstrap via cookie refresh), react-router + gardes `RequireAuth`/`RequireRole`, app-shell (header/sidebar/nav mobile), pages **Login + Home finalisées**. Gates verts. Inter chargé via `<link>` Google Fonts (pas de paquet font dispo). |
| 2026-06-19 | Lot 4 (Écrans Admin) | `apps/web/src/features/admin`, `pages/AdminPage.tsx`, `vitest.config.ts` | Back office complet : table users (états loading skeleton / empty / error), dialogue create/edit (react-hook-form + zod, mapping des codes d'erreur API), confirmation de suppression (AlertDialog), **super-admin protégé** (delete + downgrade désactivés avec tooltip via `aria-disabled` focusable), garde anti-auto-suppression, mutations react-query + toasts. 4 tests RTL verts. Table responsive (colonne « Created » masquée < 640px, pas de scroll horizontal à 375px). |
| 2026-06-19 | Lot 5 (QA / intégration / review) | racine, `apps/web/src`, `index.html` | Gate monorepo vert (lint 0 erreur / typecheck / **33 tests** : 29 API + 4 web / build). **Smoke e2e live** sur serveur réel (10/10) : login super-admin seedé, cookie httpOnly, `/me`, `/users`, delete protégé → 409, mauvais mdp → 401, non-auth → 401. **Review `web-design-guidelines` (skill 4)** : 12 correctifs a11y appliqués — CTA accessible `--color-cta #A85F00` blanc gras (**4.88:1** AA), placeholder 5.82:1, bouton désactivé 4.73:1, skip-link, `theme-color`, préload font, `touch-action`, `break-words`, overscroll popovers, ellipses placeholders, focus mobile gardé, live-region erreur login. README de lancement finalisé. **Différé (hors-scope base, documenté)** : état des dialogues dans l'URL, garde de saisie non sauvegardée. |
| 2026-06-19 | Perf (post-livraison) | `apps/web` : `app/router.tsx`, `components/layout/AppShell.tsx`, `features/auth/AuthProvider.tsx`, `vite.config.ts` | Optimisations (skill `react-best-practices`) suite au constat de lenteur. **Mesure** : backend rapide (login argon2 ~65 ms, lecture DB ~7 ms) → goulot côté front. Code-splitting par route (`lazy` + `Suspense` dans le shell) : bundle unique 554 kB → chunks `react`/`router`/`radix`/`query`/`forms`/`vendor` + 1 chunk/page ; `forms`+`AdminPage` (~29 kB gzip) différés jusqu'à `/admin`. Bootstrap session : suppression du `me()` redondant (`refresh()` renvoie déjà l'utilisateur). `optimizeDeps.include` (évite la ré-optimisation Vite à la 1re navigation) + `manualChunks` (vendors cacheables). Valeur de contexte mémoïsée. Gates verts. |
| 2026-06-19 | Perf (police + diagnostic `/`) | `apps/web/index.html`, `apps/web/src/main.tsx`, `apps/web/package.json` | **Diagnostic** du « ~2000 ms pour le document `/` » : prod `/` mesuré à **~5 ms** (preview) → les 2 s = cold-start Vite (pré-bundling des deps au 1er chargement, **dev uniquement**), pas l'app. **Correctif durable** : Inter **self-hosté** via `@fontsource/inter` (400/500/600/700 + 700 italic), suppression de la requête Google Fonts externe bloquante (~300 ms) → même origine, hors-ligne. Build : 0 référence externe dans le HTML, 35 woff2 bundlés (sous-ensemble latin à la demande). Gates verts (typecheck/lint/33 tests/build). |
| 2026-06-19 | Feature (Compte self-service) | `apps/api/src/modules/auth/*`, `apps/api/src/modules/users/users.service.ts`, `apps/api/tests/{account,users}.test.ts`, `apps/web/src/features/account/*`, `pages/AccountPage.tsx`, `lib/api/{auth,types,client}.ts`, `features/auth/{AuthProvider,auth-context}`, `app/router.tsx`, `components/layout/UserMenu.tsx` | Issu de l'analyse 5 agents (manque fonctionnel n°1 : un USER ne pouvait rien changer sur lui-même). **Backend** : `PATCH /auth/me` (nom propre uniquement ; email/rôle restent administrés), `POST /auth/change-password` (re-vérifie le mot de passe courant → 400 `INVALID_PASSWORD`, hash argon2id, **bump `tokenVersion` → révoque les AUTRES sessions** puis re-pose le cookie refresh pour garder l'appareil courant connecté). **Correctif sécu** : `users.service.updateUser` bump `tokenVersion` quand le rôle change réellement ou le mot de passe est réinitialisé (révocation quasi-immédiate d'un compte rétrogradé/réinitialisé ; un no-op de rôle ne révoque pas). **Frontend** (séquence skills obligatoire `ui-ux-pro-max → impeccable (substitut frontend-design) → design-taste-frontend → web-design-guidelines`) : page `/account` (RequireAuth, accès via le menu user en **vrai `<Link>` `asChild`**), section **Profil** (email+rôle en lecture seule via `dl`, note « managed by your administrator », nom éditable, Save désactivé tant que non modifié) + section **Mot de passe** (current/new/confirm, show/hide, zod `onBlur` : ≥8 / match / ≠ courant ; mapping `INVALID_PASSWORD` → erreur sur le champ courant), `AuthProvider.setUser` pour resync la session (avatar/menu), `client.changePassword` avec `credentials:'include'` (le serveur fait tourner le cookie). Décisions anti-slop : hairline = signature (pas de marqueur diamant répété par section), 1 CTA orange par zone. **Tests** : +13 API (`account` 9 + révocation `users` 4) + 13 web (`schemas` 8, `ProfileSection` 2, `ChangePasswordSection` 3). **Gates verts** : typecheck, lint 0 erreur, **59 tests** (42 API + 17 web), build (chunk `AccountPage` lazy ~2 kB gzip). **Différé** (cohérent avec l'app, déjà noté §lot 5) : garde de saisie non sauvegardée. |
| 2026-06-22 | Lot « App web » (backlog §11, hors track solveur) | racine (`package.json`, `.github/workflows/ci.yml`), `packages/shared/*`, `apps/api/{prisma,src,tests}`, `apps/web/src/{components,features,pages,lib,app}` | Quatre tracks du backlog livrés en un lot. **Correctifs & polish** : bug AA `sonner.tsx` `actionButton` orange `#EE7F00` → `--color-cta` (blanc gras) ; bordures d'erreur `aria-invalid` sur email+mdp du login quand `INVALID_CREDENTIALS` (effacées à la frappe, message en banner `aria-live`) ; `apps/web/.env.example` + garde *lazy* `getBaseUrl()` (erreur claire si `VITE_API_URL` absent, sans casser les tests) ; **durcissement API** : `app.set('trust proxy', env.TRUST_PROXY)`, `express.json({ limit:'16kb' })`, `config/env.ts` impose en prod des secrets JWT ≥32 char, distincts, non-placeholder (+ `SEED_ADMIN_PASSWORD`). **Robustesse** : pipeline **CI** GitHub Actions (prisma generate → build:shared → lint → typecheck → test → build) ; table **`AuditLog`** (append-only, acteur/cible dénormalisés) + `lib/audit.ts` best-effort, enregistrée sur login/logout/profil/mdp et create/update/delete/disable/enable ; `GET /audit-logs` (super-admin, paginé) + page **/activity**. **Profondeur back-office** : migration Prisma `add_audit_log_and_account_status` (`User.isActive`, `User.lastLoginAt`) ; login stampe `lastLoginAt` et **refuse 403 `ACCOUNT_DISABLED`** (après vérif mdp, pas d'énumération) ; `requireAuth`+`refresh` rejettent un compte désactivé ; `updateUser` gère `isActive` (désactiver bump `tokenVersion`, garde `PROTECTED_ACCOUNT` + nouvelle `SELF_DISABLE_FORBIDDEN`) ; table admin **recherche** (nom+email, Échap efface) + **tri** colonnes (`aria-sort`) + colonne **Last login** + badge **Active/Disabled** (icône+texte) + action **Disable/Enable** (confirm dialog pour désactiver, direct pour réactiver) ; séquence skills obligatoire suivie (`ui-ux-pro-max → design-taste-frontend → web-design-guidelines`). **Contrat partagé** : `packages/shared` (`@dive/shared`, dual CJS+ESM) source unique de `Role`/`roleSchema`/`PASSWORD_MIN/MAX`/`FULL_NAME_MAX`/`SERVER_ERROR_CODES`, consommé par les schémas zod API + web (supprime la dérive de longueur de mdp). **Garde de saisie** : `UnsavedChangesPrompt` (`useBlocker` + `beforeunload`) sur `/account` (état dirty agrégé des 2 sections) et le dialogue admin create/edit. **Gates verts** : lint 0 erreur, typecheck, **82 tests** (55 API + 27 web), build (chunks `AdminPage`/`ActivityPage` lazy). Note : pré-requis CI/local `npm run build:shared` avant typecheck/test/dev (intégré aux scripts racine). |
| 2026-06-22 | Feature (Projects) | `packages/shared/src/index.ts`, `apps/api/{prisma,src/modules/projects,tests/projects.test.ts,app.ts,tests/helpers.ts}`, `apps/web/src/{lib/api,features/projects,pages/ProjectsPage.tsx,components/layout/nav.ts,app/router.tsx}` | Zone **Projects** : un utilisateur connecté crée un projet (titre seul, rien d'autre). **Backend** : modèle Prisma `Project` (titre, `ownerId` relation `User` `onDelete: Cascade`) + migration `add_project` ; module `projects` (requireAuth, **owner-scoped**) — `POST /projects` (zod `title` 1..`PROJECT_TITLE_MAX_LENGTH` partagé) + `GET /projects` (les siens, plus récents d'abord) ; `ownerId` jamais sérialisé. **Frontend** (système DESIGN.md déjà cadré par la séquence skills cette session) : nav item **Projects** (tous les rôles), route `/projects` lazy, page = 1 form de création (champ titre + 1 CTA orange) au-dessus de la liste (états loading skeleton / empty / error / data ; colonne Created `tabular-nums`). **Tests** : +4 API (auth requise, création 201 owner-scopée, titre vide 422, isolation entre users) + 3 web (schéma titre). `resetDatabase` purge `project` aussi. **Gates verts** : typecheck, lint 0 erreur, **89 tests** (59 API + 30 web), build (chunk `ProjectsPage` lazy ~1.9 kB gzip). |
| 2026-06-22 | Feature (Projects: collaborateurs, détail, delete) | `packages/shared/src/index.ts`, `apps/api/{prisma,src/modules/projects/*,tests/projectsAccess.test.ts}`, `apps/web/src/{lib/api/{types,projects},features/projects/useProjects,pages/{ProjectsPage,ProjectDetailPage},app/router.tsx}` | Évolution du modèle d'accès des projets. **Visibilité** : un projet est visible par son **owner**, ses **collaborateurs**, et **tout super-admin** ; un étranger reçoit **404** (pas de fuite d'existence). **Migration** `add_project_collaborators` : m2m implicite `Project.collaborators ↔ User` (relations nommées `ProjectOwner`/`ProjectCollaborators`). **Backend** : `GET /projects` (owner+collab ; super-admin → tout), `GET /projects/:id` (404 si non-membre), `DELETE /projects/:id` (**owner ou super-admin** ; un simple collaborateur → 403 `FORBIDDEN`), `POST /projects/:id/collaborators` (ajout **par email** ; 404 `USER_NOT_FOUND`, 409 `COLLABORATOR_EXISTS` si owner/déjà collab), `DELETE /projects/:id/collaborators/:userId`. `PublicProject` sérialise désormais `owner` + `collaborators` (résumés `{id,fullName,email}`). Nouveaux codes partagés `USER_NOT_FOUND`/`COLLABORATOR_EXISTS`. **Frontend** : liste = titres **cliquables** (lien vers `/projects/:id`) + colonne **Owner** (« You » si c'est l'utilisateur) ; **page détail** `/projects/:id` (lazy) avec section Détails (owner, date) + section Collaborateurs (ajout par email + retrait, réservés owner/super-admin via `canManage` = owner || super-admin) + bouton **Delete project** (déclencheur secondaire teinté danger → dialogue de confirmation destructif), état not-found dédié. **Tests** : +14 API (visibilité owner/collab/étranger/super-admin, détail 404, delete 204/403/404/super-admin, ajout/retrait collaborateur, email inconnu, doublon, non-manager 403). **Gates verts** : typecheck, lint 0 erreur, **103 tests** (73 API + 30 web), build (chunk `ProjectDetailPage` lazy ~2.6 kB gzip). Note Windows : régénérer le client Prisma exige d'arrêter le dev server (`tsx watch` verrouille le moteur). |
| 2026-06-22 | Tweak UI (déplacement delete + retrait visuel Activity) | `apps/web/src/pages/ProjectDetailPage.tsx`, `apps/web/src/{components/layout/nav.ts,app/router.tsx,lib/api/types.ts}` (+ suppressions) | **(1)** Sur demande `/ui-ux-pro-max` : le bouton **Delete project** quitte le header (slot du CTA principal) pour une section **« Danger zone »** en bas de la page détail (bordure `border-danger/40`, libellé + ligne explicative), conforme à `destructive-nav-separation` / `destructive-emphasis`. **(2)** Retrait de la **partie visuelle Activity** : suppression de `pages/ActivityPage.tsx`, `features/admin/{useAuditLogs,auditActions}.ts`, `lib/api/audit.ts`, du nav item + route `/activity`, et des types `AuditLogEntry`/`ListAuditLogsResponse`. **Backend conservé** (non visuel) : table `AuditLog`, enregistrement, `GET /audit-logs` + ses tests. **Gates verts** : typecheck, lint 0 erreur, **103 tests** (73 API + 30 web), build (plus de chunk `ActivityPage`). |
| 2026-06-22 | Feature (Projects: import de cas OpenFOAM) | `packages/shared`, `apps/api/{.env*,vitest.config,src/config/env,src/lib/{caseStorage,openfoamCase},src/modules/projects/{files.service,files.controller,projects.routes,projects.service},tests/{openfoamCase,projectFiles}.test.ts}`, `apps/web/src/{lib/api/{client,types,projects},features/projects/{useCaseFiles,CaseFilesSection,CaseFilesSection.test},pages/ProjectDetailPage}`, `.gitignore` | **Premier pas du track solveur** : importer un cas OpenFOAM dans un projet, le visualiser, le télécharger, et auto-générer les fichiers de base manquants (cf. `docs/openfoam-fichiers-obligatoires.md`). Le serveur OpenFOAM n'existe pas encore → **100 % applicatif** (aucun appel `fluentMeshToFoam`/solveur). **Stockage** : filesystem par projet sous `STORAGE_DIR` (`apps/api/storage/projects/<id>/case/`), **anti-traversal + anti-zip-slip** (sanitize `..`/absolu/drive, `confineJoin`), **cross-platform** (forward-slash logique, `path.sep` natif) — l'app finira sur **Linux**. Multer 2.x (`preservePath: true` pour garder le chemin relatif du dossier — busboy strip sinon) + adm-zip. **Normalisation d'import** : un dossier `polyMesh` seul atterrit sous `constant/polyMesh/`, un wrapper (nom du dossier sélectionné / dossier de cas) est retiré. **Endpoints** (auth + visibilité projet, membres inclus) : `GET /projects/:id/files` (arbre), `POST .../files/import` (dossier `files[]` ou zip `archive`), `GET .../files/download` (zip), `GET .../files/verify` (rapport mesh + base scaffoldable), `POST .../files/scaffold` (génère `system/{controlDict,fvSchemes,fvSolution}` + `0/{U,p}` génériques minimaux, `boundaryField` câblé sur les patches lus dans `constant/polyMesh/boundary`, jamais d'écrasement). Storage nettoyé au delete projet (best-effort). Nouveaux error codes partagés `NO_FILES_UPLOADED`/`INVALID_ARCHIVE`/`PAYLOAD_TOO_LARGE` + `CASE_UPLOAD_MAX_BYTES` (200 Mo). **Frontend** (séquence skills `CLAUDE.md §0` suivie : `ui-ux-pro-max → design-taste-frontend → web-design-guidelines`) : `client.ts` étendu (`postForm` multipart + `getBlob` réutilisant le refresh single-flight), hooks `useCaseFiles`, section **Case files** sur `/projects/:id` — états loading skeleton / empty (prompt d'import, 1 CTA orange = dossier) / error / data (toolbar import dossier+zip / download / **Verify**), arbre indenté (icônes lucide, tailles `tabular-nums`, `truncate`+`title`), et **overlay** (AlertDialog `overscroll-contain`) qui s'ouvre sur Verify quand des fichiers de base manquent → « Create files » (CTA `--color-cta` AA) appelle scaffold. **Tests** : +25 API (12 unité `openfoamCase` : parse boundary, templates, normalisation/anti-traversal ; 13 intégration `projectFiles` : import dossier/zip, zip-slip 400, verify, scaffold + non-écrasement, download 404/zip, accès collaborateur/super-admin) + 3 web (empty / arbre / overlay). **Gates verts** : typecheck, lint 0 erreur, **131 tests** (98 API + 33 web), build (chunk `ProjectDetailPage` ~16 kB). Note : `npm i -w @dive/api multer@^2 adm-zip` (+ `@types`). **Différé** : conversion réelle du maillage (Ansys/Fluent → polyMesh) et reste du cas (`constant/...Properties` selon solveur) quand le serveur distant existera ; drag-and-drop d'import. |
| 2026-06-22 | Tweak UI (engrenage settings projet) + fix limite upload | `apps/web/src/pages/ProjectDetailPage.tsx` ; (+ fix : `apps/api/{.env*,vitest.config? non,src/config/env,src/modules/projects/files.controller}`, `packages/shared/src/index.ts`) | **(1) Engrenage de réglages** (`/ui-ux-pro-max` → recommandation : menu plutôt que dialog combiné, conforme `overflow-menu`/`destructive-nav-separation`/`destructive-emphasis`/`progressive-disclosure`). Bouton **gear** (`Settings`, ghost icon, `aria-label`, **owner/super-admin only**) dans le slot action du `PageHeader` → **DropdownMenu** : « Manage collaborators » (→ **Dialog** : form ajout par email avec 1 CTA orange + liste + retrait) **/ séparateur /** « Delete project » (item `destructive` → **AlertDialog** de confirmation). Ouverture des overlays **différée** (`setTimeout 0`) pour éviter la course focus/aria-hidden au retour-focus du menu Radix. La page garde une **liste collaborateurs en lecture seule** (visible par tous → overview), la gestion (add/remove) et le delete passent derrière l'engrenage ; l'ancienne « Danger zone » en bas de page et le form inline sont retirés. **(2) Fix** (signalé en run) : la limite multer **200 Mo/fichier** rejetait de vrais maillages CFD (`413 PAYLOAD_TOO_LARGE`) → **`MAX_UPLOAD_MB`** (env, défaut **1024**) pilote `limits.fileSize` ; constante partagée `CASE_UPLOAD_MAX_BYTES` retirée. **Gates verts** : typecheck, lint 0 erreur, **131 tests** (98 API + 33 web), build (chunk `ProjectDetailPage` ~17 kB). Note mémoire : uploads bufferisés en RAM (memoryStorage) → pour des maillages multi-Go, passer en `diskStorage` streaming (différé). |
| 2026-06-22 | Feature (Édition de fichiers de cas) + retrait liste collaborateurs page | `packages/shared/src/index.ts`, `apps/api/src/modules/projects/{files.service,files.controller,files.schemas(new),projects.routes}.ts`, `apps/api/tests/projectFiles.test.ts`, `apps/web/src/{lib/api/{client,types,projects},features/projects/{useCaseFiles,CaseFileEditor(new),CaseFilesSection},pages/{ProjectEditPage(new),ProjectEditPage.test,ProjectDetailPage},app/router.tsx,vite.config.ts}` | Étape « modifier les paramètres avant le solve ». **(A)** Retrait de la liste collaborateurs en lecture seule de la page détail (déjà gérée via l'engrenage) → page = Details + Case files. **(B) Éditeur de fichiers** (plan validé ; séquence skills `CLAUDE.md §0` suivie, choix éditeur = **CodeMirror** validé par l'utilisateur). **Backend** : `EDITABLE_FILE_MAX_BYTES` (2 Mo) + code `FILE_TOO_LARGE` partagés ; `readCaseFileContent` (404 absent / 413 trop gros) + `saveCaseFileContent` (édition d'un fichier **existant** uniquement, 404 sinon ; cap 413) réutilisant `readCaseFile`/`writeCaseFile`/`caseFileExists` (déjà path-safe) + `assertProjectVisible` ; `GET`/`PUT /projects/:id/files/content?path=` — le `PUT` a son **propre** parser `express.text({type:'*/*', limit})` car le `json` global est à 16 ko ; le client envoie le contenu en `text/plain`. **Frontend** : `apiClient.putText` (corps texte brut, réutilise le refresh 401) ; hooks `useCaseFileContentQuery`/`useSaveCaseFile` ; bouton **« Edit files »** (secondaire) dans la toolbar Case files → route lazy `/projects/:id/edit` ; page **deux volets** (liste fichiers à gauche / éditeur CodeMirror `cpp()` à droite), Save = unique CTA orange (désactivé tant que non *dirty*), états no-selection (diamant) / loading skeleton / **413 trop-gros** (notice + download) / error ; garde `UnsavedChangesPrompt` (quitter la page) + dialogue **discard** au changement de fichier si édits non sauvés. `CaseFileEditor` isolé (dép. lourde) + **`manualChunks` dédié `codemirror`** (522 ko, chargé seulement avec la page d'édition ; `vendor` retombe à 108 ko) + `optimizeDeps`. **Tests** : +6 API (get/put content : 200/404/413/traversal/save-readback) + 3 web (liste, sélection charge le contenu, save) — éditeur mocké en `<textarea>`, data-router pour `useBlocker`. **Gates verts** : typecheck, lint 0 erreur, **140 tests** (104 API + 36 web), build (chunk `ProjectEditPage` ~8 ko + `codemirror` lazy). Dép. : `npm i -w @dive/web @uiw/react-codemirror @codemirror/lang-cpp`. **Différé** : création de nouveaux fichiers via l'éditeur (édition only), conversion réelle du maillage + solve. |
| 2026-06-22 | Tweak UI (éditeur plein écran + autosave) | `apps/web/src/components/layout/AppShell.tsx`, `apps/web/src/pages/{ProjectEditPage,ProjectEditPage.test}.tsx` | Sur retour utilisateur. **(1) Plein écran** : `AppShell` détecte la route `/projects/:id/edit` (regex sur `useLocation`) et rend le contenu **full-bleed** (sans `max-w-content` ni centrage, `main`/wrapper en `flex flex-col flex-1 min-h-0`) ; la page d'édition devient une colonne flex pleine hauteur (`lg:flex-1 lg:min-h-0`), volet fichiers + éditeur remplissent le viewport (hauteur définie `h-[60vh]` sur mobile, `lg:flex-1` sur desktop pour que CodeMirror `height:100%` se résolve). Les autres pages restent centrées à 1200px. **(2) Autosave** : sauvegarde **débouncée 600 ms** après la dernière frappe (plus de bouton Save) ; le flush est déclenché aussi au changement de fichier (zéro perte) ; indicateur de statut `aria-live` (« Saving… » / « All changes saved » / « Save failed » + Retry) à la place du CTA ; suppression du dialogue de discard (autosave). Garde `UnsavedChangesPrompt` conservée pour la fenêtre avant flush / échec réseau. **Gates verts** : typecheck, lint 0 erreur, **140 tests** (104 API + 36 web), build. |
| 2026-06-22 | Tweak UI (éditeur : retrait titre + scroll interne) | `apps/web/src/components/layout/AppShell.tsx`, `apps/web/src/pages/{ProjectEditPage,ProjectEditPage.test}.tsx` | Retour utilisateur. **(1)** Retrait du `PageHeader` (titre projet + « Edit case files ») de la page d'édition → gain de hauteur, le chemin du fichier en haut du volet éditeur suffit comme contexte ; le `BackLink` reste. **(2) Scroll interne** : la page d'édition est désormais **bornée à la hauteur du viewport** pour que seul CodeMirror scrolle (pas la page). Dans `AppShell`, `min-h-0` + `overflow-hidden` ne s'appliquent qu'à la route full-bleed via un `mainClass` conditionnel — **pas** sur les pages normales (sinon leur contenu long serait coupé au lieu de scroller). Test ajusté (plus d'assertion sur le titre). **Gates verts** : typecheck, lint 0 erreur, **140 tests**, build. |
| 2026-06-22 | Fix (éditeur : page scrollait encore) | `apps/web/src/components/layout/AppShell.tsx`, `apps/web/src/pages/ProjectEditPage.tsx` | La chaîne `min-h-0 flex-1` ne bornait pas réellement : le volet éditeur était un **item de CSS grid à ligne implicite `auto`** (taille = contenu), donc aucune hauteur définie ne descendait jusqu'à CodeMirror → l'éditeur prenait la taille du fichier et la **page** grandissait/scrollait. **Correctif déterministe** : le conteneur full-bleed est épinglé à `h-[calc(100dvh_-_4rem)]` (viewport − header `h-16` 64px, vérifié compilé en `calc(100dvh - 4rem)`) + `overflow-hidden` → la page ne peut plus scroller ; `main` **rétabli** à `min-w-0 flex-1` (son `min-content` = la hauteur fixe réserve exactement l'espace → zéro débordement, et aucune régression sur les pages normales longues qui doivent scroller). Le **two-pane passe de grid à flex** (`flex-col lg:flex-row`) car `flex` (align stretch) propage une hauteur définie là où une ligne grid `auto` ne le fait pas ; volets en `min-h-0 flex-1`, liste fichiers `overflow-auto`, conteneur éditeur `min-h-0 flex-1` → CodeMirror `height:100%` se résout et scrolle en interne. Suppression du bricolage `h-[60vh] lg:h-auto`. **Gates verts** : typecheck, lint 0 erreur, **140 tests**, build. |
| 2026-06-22 | Feature (détail plein écran + Reset des fichiers) | `apps/web/src/components/layout/AppShell.tsx`, `apps/web/src/pages/ProjectDetailPage.tsx`, `apps/web/src/features/projects/{useCaseFiles,CaseFilesSection,CaseFilesSection.test}.tsx`, `apps/web/src/lib/api/projects.ts`, `apps/api/src/{lib/caseStorage,modules/projects/{files.service,files.controller,projects.routes}}.ts`, `apps/api/tests/projectFiles.test.ts` | **(1) Détail plein écran** : `AppShell` distingue deux modes projet — éditeur (`…/edit`, hauteur fixe + scroll interne, déjà en place) **et** détail (`/projects/:id`, **pleine largeur mais scroll de page normal** : `w-full` sans `max-w-content`). La page détail perd son cap interne `max-w-3xl` → contenu pleine largeur. Les autres pages restent centrées. **(2) Reset** : suppression de tous les fichiers importés (« si je me suis trompé »). Backend `DELETE /projects/:id/files` (auth + visibilité, membre OK comme l'import) → `clearCase` (rm récursif du dossier `case`, recréé au prochain import) → renvoie l'arbre vide. Frontend : bouton **Reset** (danger outline, icône `RotateCcw`) visible seulement quand des fichiers existent, dans le groupe droit de la toolbar à côté de Verify (séparé visuellement, couleur danger), confirmation `AlertDialog` destructive ; au succès le cache vide → retour automatique à l'état import. Hook `useResetCase` + `resetCase` API. **Tests** : +2 API (reset 200 + arbre vide, auth 401) + 1 web (Reset → dialogue → `resetCase`). **Gates verts** : typecheck, lint 0 erreur, **143 tests** (106 API + 37 web), build. Note : Reset autorisé aux membres (canView, cohérent avec import/edit) ; à restreindre aux managers si besoin plus tard. |
| 2026-06-22 | Feature (Case files : onglets Files / Summary + synthèse par fichier) | `apps/web/src/components/ui/tabs.tsx` (new), `apps/web/src/features/projects/{foamSummary.ts,foamSummary.test.ts,CaseSummary.tsx}` (new), `apps/web/src/features/projects/{CaseFilesSection,CaseFilesSection.test}.tsx`, `apps/web/package.json` | Demande : dans la zone **Case files** du détail projet, un **système d'onglets** — onglet 1 « **Files** » = la vue import/gestion existante **inchangée** (import dossier/zip, arbre, verify, download, edit, reset) ; onglet 2 « **Summary** » = une **synthèse par fichier** des réglages saisis, pour un coup d'œil rapide. Séquence skills `CLAUDE.md §0` suivie (`ui-ux-pro-max → impeccable (substitut frontend-design) → design-taste-frontend → web-design-guidelines`). **100 % frontend**, aucun changement backend. **Onglets** : primitive `components/ui/tabs.tsx` sur `@radix-ui/react-tabs` (cohérent avec les autres primitives Radix), style *underline* in-card tokenisé — actif = texte **`--primary`** (sélection courante) + soulignement 2px + semibold, **jamais l'orange** (réservé au CTA, principe « scalpel ») ; indicateur = couleur **+** graisse **+** trait (jamais la couleur seule) ; Radix fournit roving-tabindex / flèches / Home / End + ARIA `tablist/tab/tabpanel`. Les inputs cachés + l'overlay « generate files » restent au niveau section (montés quel que soit l'onglet). **Synthèse** (`CaseSummary.tsx`) : filtre = fichiers **ouvrables** (`≤ EDITABLE_FILE_MAX_BYTES` 2 Mo) **hors mesh** (`constant/polyMesh/*`, données machine, non « saisies ») ; contenu chargé via `useQueries` en **réutilisant `caseFileContentQueryKey`** (cache partagé avec l'éditeur). **Parser dédié** `foamSummary.ts` (tokenizer + descente récursive **tolérante** : strip bannière/commentaires, `{}` seuls structurels, `()`/`[]` gardés dans la valeur, en-tête `FoamFile` → méta `class`/`object`). Rendu : groupes par fichier séparés par **hairlines** (`divide-y`) — **pas de filet sous chaque ligne** (anti-slop `design-taste §4.9`) ; en-tête (chemin mono `dir` muté + nom, badge `class`, compteur « N entries » `tabular-nums`) puis `dl/dt/dd` clé→valeur mono, sous-dictionnaires indentés sous un **guide 1px** (`border-l`, hairline neutre, pas un side-stripe coloré). **États** complets : skeleton (arbre + par fichier), erreur arbre (`role="alert"` + retry), **empty qui enseigne** (losange, message selon mesh-only vs cas vide), erreur **par fichier** (icône danger + Retry, tolérance partielle), note « trop gros », note « N fichiers mesh/lourds non synthétisés ». **A11y (review skill 4)** : `dl > div > dt/dd` valide (`FoamBlock` repassé en `dt/dd`), focus visibles, *color-not-only*, contrastes AA, **`translate="no"`** sur les jetons de code (chemins, `class`, clés/valeurs) — pas sur la copie d'UI. **Déviation assumée** cohérente avec l'app : état d'onglet en `useState` (pas en URL), comme les dialogues (URL-state déjà différé §lot 5). **Dép.** : `npm i -w @dive/web @radix-ui/react-tabs`. **Tests** : +9 unité (`foamSummary` : méta, dimensions/vecteurs, imbrication + clés regex, boundaryField, commentaires, bloc vide, entrée vide, bloc non terminé, `countFoamEntries`) + 2 web (onglets présents + défaut `Files` ; Summary rend les réglages parsés et **ignore le mesh**) ; `getCaseFileContent` ajouté au mock, clic onglet via **`userEvent`** (Radix s'active au focus/pointer, pas via `fireEvent.click`). **Gates verts** : typecheck, lint **0 erreur**, **154 tests** (106 API + 48 web), build (chunk `ProjectDetailPage` 17 → **25,6 ko** / 7,27 ko gzip). **Différé** : pas de parsing du format *liste* de `constant/polyMesh/boundary` (mesh exclu de la synthèse). |
| 2026-06-22 | Tweak UI (toolbar Case files) | `apps/web/src/features/projects/CaseFilesSection.tsx` | Retour utilisateur. **(1)** Inversion de l'ordre **Edit files** ↔ **Download** dans la toolbar de l'onglet *Files* → Import dossier / Import .zip / **Edit files** / **Download** / *(ml-auto)* Reset / Verify. **(2)** Texte du bouton **Download** passé en **orange** via le token `text-cta` (`--color-cta #A85F00`, **4,88:1 AA**) + `hover:text-cta-hover` — **pas** l'orange vif `#EE7F00` (`--color-accent`), qui échoue l'AA en petit texte sur blanc (~2,74:1, règle DESIGN.md §2). Bouton conservé `ghost` (le seul orange *plein* de la zone reste le CTA **Verify**, principe « scalpel »). **Gates verts** : typecheck, lint 0 erreur, **154 tests**, build. |
| 2026-06-22 | Feature (Templates réutilisables partagés + create/delete fichier dans l'éditeur) | **API** : `packages/shared/src/index.ts` (codes `FILE_EXISTS`, constantes `TEMPLATE_NAME/DESCRIPTION_MAX_LENGTH`), `apps/api/src/lib/{fileTreeStorage(new),caseStorage(façade),templateStorage(new)}.ts`, `apps/api/src/modules/projects/{files.service,files.controller,files.schemas,projects.routes}.ts`, `apps/api/src/modules/templates/*(new)`, `apps/api/src/{app.ts,modules/users/users.service.ts}`, `apps/api/prisma/schema.prisma` (+migration `add_template`), `apps/api/tests/{templates.test(new),projectFiles.test,helpers}.ts`. **Web** : `apps/web/src/lib/api/{types,projects,templates(new)}.ts`, `apps/web/src/features/projects/useCaseFiles.ts`, `apps/web/src/features/templates/*(new : useTemplates, schemas, TemplateFormDialog, ApplyTemplateFlow)`, `apps/web/src/features/files/FileTreeEditor(new).tsx`, `apps/web/src/pages/{ProjectEditPage(wrapper),TemplateEditPage(new),TemplatesPage(new)}.tsx`, `apps/web/src/components/ui/textarea(new).tsx`, `apps/web/src/components/layout/{nav.ts,AppShell.tsx}`, `apps/web/src/app/router.tsx`, `apps/web/src/features/projects/CaseFilesSection{,.test}.tsx`. **(A) Ajout mineur — create/delete fichier dans l'éditeur** : `saveCaseFileContent` n'éditait que l'existant. Ajout `createCaseFile` (409 `FILE_EXISTS`, 413) + `deleteCaseFileContent` (404) + routes `POST`/`DELETE /projects/:id/files/content` (distinctes du `DELETE /:id/files` reset) ; UI « New file » (dialog chemin) + « Delete » (AlertDialog) dans l'éditeur. **(B) Ajout majeur — templates partagés** (décisions user : **partagés entre tous** avec auteur ; application = **demander à chaque conflit**, défaut *keep* ; contenu = **arbre libre**). Modèle Prisma `Template` (owner `onDelete: Cascade` + purge disque dans `deleteTemplate` **et** `deleteUser`). Socle storage factorisé : `fileTreeStorage` (racine paramétrable, path-safe, `deleteFileAt`+prune) ; `caseStorage` devient une façade (API publique inchangée, normalisation OpenFOAM conservée) ; `templateStorage` (`<STORAGE>/templates/<id>/files`, normalisation libre = sanitize seul). Module `templates` complet (CRUD, fichiers list/import/content r-w-create-delete, **lecture/application ouvertes à tout user, mutations = auteur/super-admin**) ; application **2 temps** montée côté projet (`GET …/apply-template/:tid/preview`, `POST …/apply-template/:tid`, autorisation = **visibilité projet**, conflits **recalculés serveur**, copie binaire sans cap). **Flux Verify** étendu : `Verify case` ouvre un dialogue 3 choix — (a) **template minimal intégré** (= scaffold), (b) **choisir un template enregistré** → preview → résolution des conflits **par fichier** (Overwrite/Keep) → apply, (c) **Ignorer**. Nouvel onglet/nav **Templates** (`/templates`, `/templates/:id/edit`) ; éditeur de template = même `FileTreeEditor` (extrait de `ProjectEditPage`, libellés/aria préservés) + import dossier/zip ; éditeur full-bleed étendu à `…/templates/:id/edit`. Charte CLAUDE.md respectée (tokens, 1 CTA orange/zone, AA, états loading/empty/error, focus, `translate="no"`). **Note** : la *working tree* du container arrivait avec un correctif **partiel non commité** (onglets Files/Summary) référençant `components/ui/tabs.tsx`, `features/projects/{foamSummary.ts,CaseSummary.tsx}` **absents** → build cassé avant toute intervention ; ces 3 fichiers ont été **recréés** (Radix tabs tokenisé + parser FOAM tolérant + synthèse) pour rétablir le build (le `foamSummary.test.ts` mentionné plus haut n'a pas été recréé). **Tests** : +14 API (`templates` : CRUD/droits, fichiers, preview/apply décisions, collaborateur), +6 API (`projectFiles` : create 201/409/422, delete 200/404/traversal), +2 web (`CaseFilesSection` : flux 3 choix + scaffold minimal). **Gates verts** : `build:shared`, lint **0 erreur**, typecheck, **166 tests** (126 API + 40 web), build (chunks `TemplatesPage`/`TemplateEditPage`/`FileTreeEditor`/`useTemplates` lazy). **Différé** : pas de duplication de template ; pas de réordonnancement d'arbre ; `foamSummary.test.ts` non recréé. |
| 2026-06-22 | Tweak UI (onglet Summary : scroll confiné au tableau) | `apps/web/src/features/projects/CaseSummary.tsx` | Retour utilisateur : dans l'onglet **Summary** du détail projet, **toute la page** scrollait au lieu du seul tableau. Le conteneur de la liste de synthèse devient une **région scrollable bornée** (`max-h-96 overflow-y-auto`), cohérent avec l'onglet **Files** dont l'arbre est déjà en `max-h-80 overflow-auto`. Séquence skills `CLAUDE.md §0` suivie (`ui-ux-pro-max` cadrage → `web-design-guidelines` review ; étapes 2-3 non pertinentes pour un fix de confinement de scroll, aucun nouveau visuel). **A11y (review skill 4)** : région **focusable au clavier** (`tabIndex={0}` — sans enfant focusable le scroll clavier serait impossible) avec **nom accessible**, élément **`<section aria-label>` natif** plutôt que `div role="region"` (règle *Semantic HTML before ARIA*), anneau **`focus-visible`** (remplace `outline-none`), **`overscroll-contain`** (le scroll ne se chaîne pas à la page une fois les bords atteints). Pas de valeur Tailwind arbitraire (classe standard `max-h-96`). **Gates verts** : typecheck web + **40 tests web**. **Différé** (cohérent, hors périmètre) : l'onglet *Files* conserve le même léger manque d'a11y clavier sur sa propre région scrollable (pré-existant) ; alignable si souhaité. |
| 2026-06-23 | Feature (Templates : import dossier → overlay « choisir ce qu'on garde ») | `apps/web/src/features/files/{folderImport.ts,FolderImportDialog.tsx,folderImport.test.ts}` (new), `apps/web/src/pages/TemplateEditPage.tsx` | Demande utilisateur : dans l'éditeur de **template**, pouvoir importer **plusieurs dossiers à la fois**. Le picker natif `webkitdirectory` ne sélectionne **qu'un** dossier par dialogue (limite navigateur) → solution retenue (validée user) : importer **un dossier parent** puis un **overlay de sélection** des sous-dossiers/fichiers à conserver. **100 % frontend**, aucun changement backend ni API (les chemins relatifs `webkitRelativePath` sont déjà préservés ; on **filtre** simplement les `File[]` avant l'upload existant). `folderImport.ts` : `groupPickedFolder` regroupe la liste plate par **enfant immédiat** sous la racine (dossiers d'abord puis fichiers, tri alpha ; `[root,child,…]`=dossier, `[root,file]`=fichier libre). `FolderImportDialog.tsx` : `Dialog` Radix tokenisé, **case native par ligne** dans un `<label>` (hit-target unique, `accent-primary` — pas l'orange, réservé CTA), **Select all/Deselect all**, liste scrollable bornée `max-h-72 overflow-auto overscroll-contain`, footer **Cancel** + CTA **« Import N files »** (désactivé si 0 sélectionné, spinner+disabled via `loading`). `TemplateEditPage` : le pick ouvre l'overlay au lieu d'importer ; **.zip inchangé** (import direct). Séquence skills `CLAUDE.md §0` suivie (`ui-ux-pro-max` cadrage → motif « boîte/arbre » déjà établi dans le code réutilisé, anti-slop : pas de dégradé/glass, ombre douce, tokens seuls → `web-design-guidelines` review : labels, `<button>` sémantiques, `focus-visible`, `aria-busy`, **`overscroll-contain` ajouté**). **A11y** : dialogue focus-trap Radix (Esc/scrim), chaque ligne `<label>`+checkbox natif, anneaux `focus-visible`. **Tests** : +4 web (`groupPickedFolder` : vide, regroupement par enfant, ordre dossiers-puis-fichiers, fallback nom de fichier). **Gates verts** : typecheck, lint **0 erreur** (aucun warning sur les nouveaux fichiers), tests web. **Différé** : sélection **niveau 1** uniquement (pas d'arbre profond cochable) ; même overlay non encore appliqué à l'import de cas projet (`CaseFilesSection`) — alignable si souhaité. |
| 2026-06-23 | Tweak UI (import dossier : retirer le dossier parent + option) | `apps/web/src/features/files/{folderImport.ts,FolderImportDialog.tsx,folderImport.test.ts}`, `apps/web/src/lib/api/templates.ts`, `apps/web/src/features/templates/useTemplates.ts`, `apps/web/src/pages/TemplateEditPage.tsx` | Retour utilisateur : importer `OFSolverExample/` gardait le **dossier parent** dans les chemins (`OFSolverExample/0/U`). Désormais le root du pick est **retiré par défaut** (→ `0/U`), cohérent avec l'overlay qui liste déjà les enfants `0/constant/system`. L'upload passe de `File[]` à **`UploadFile[]` (`{ file, path }`)** : `uploadPath(file, keepRoot)` calcule le chemin (slice du 1ᵉʳ segment ; fichier sans préfixe inchangé), `importTemplateFolder` envoie `path` comme nom multipart (le serveur le prend tel quel via `originalname` — **aucun changement backend**). Case **« Keep the OFSolverExample folder »** (décochée par défaut, `accent-primary`) pour conserver le wrapper au besoin, avec libellé d'état dynamique. **Tests** : +3 web (`uploadPath` : strip défaut, keep, fichier nu). **Gates verts** : typecheck, lint 0 erreur, tests web. |
| 2026-06-23 | Feature (Éditeur de fichiers : déplacer (drag & drop + menu) et supprimer des dossiers) | **API** : `apps/api/src/lib/{fileTreeStorage,caseStorage,templateStorage}.ts`, `apps/api/src/modules/projects/{files.service,files.controller,files.schemas,projects.routes}.ts`, `apps/api/src/modules/templates/{templates.service,templates.controller,templates.routes}.ts`, `apps/api/tests/{projectFiles,templates}.test.ts`. **Web** : `apps/web/src/lib/api/{types,projects,templates}.ts`, `apps/web/src/features/projects/useCaseFiles.ts`, `apps/web/src/features/templates/useTemplates.ts`, `apps/web/src/features/files/FileTreeEditor.tsx`, `apps/web/src/pages/{ProjectEditPage,TemplateEditPage}.tsx`, `apps/web/src/pages/ProjectEditPage.test.tsx`. Demande utilisateur (décisions via `AskUserQuestion` : interaction = **glisser-déposer**, périmètre = **templates + cas projet** car éditeur partagé). **Storage** (cœur factorisé) : `pruneEmptyParents` extrait de `deleteFileAt` ; nouveaux `deleteDirAt` (rm -rf confiné + prune, 404 si absent/fichier) et `moveAt` (rename path-safe, crée les parents dest, prune source ; 404 source absente, 409 `FILE_EXISTS` dest occupée, 400 `VALIDATION_ERROR` dossier-dans-lui-même) ; exposés via façades `case`/`template`. **API** : 2 endpoints × 2 ressources — `DELETE …/files/dir?path=` (suppression dossier) et `POST …/files/move` `{from,to}` (déplacer/renommer), schéma `movePathSchema` partagé ; droits = visibilité projet (cas) / auteur·super-admin (templates), conflits/chemins **recalculés serveur** (path-safe). **Web** : `FileTreeResource` étendu (`useDeleteDir`, `useMove`) câblé des deux côtés ; hooks invalident l'arbre + purgent les caches de contenu sous le préfixe. **UI arbre** (`FileTreeEditor`) : lignes **draggables** (poignée `GripVertical` au survol), **dossiers = cibles de drop** (surbrillance `primary`), la liste elle-même = cible **« niveau racine »** (anneau pendant le drag) ; garde-fous UI (pas de dossier dans lui-même / pas de no-op). **Fallback clavier (a11y, exigé charte)** : menu par ligne (`DropdownMenu` Radix) « **Move to** » (racine + chaque dossier valide) + « **Delete file/folder** » destructif → confirmation `AlertDialog` unique (fichier ou dossier). Sélection ouverte **resynchronisée** après move/delete (remap chemin / désélection). Tokens seuls, 1 seul rouge destructif, `overscroll-contain`, focus visibles, `aria-label` sur le kebab. **Tests** : +6 API cas (`files/dir` ok/404 ; `files/move` fichier/dossier/409/400/404 ; fixtures avec frère top-level pour contourner le strip wrapper OpenFOAM), +4 API templates (delete-dir, move, **403 étranger** sur move & dir) ; test `ProjectEditPage` durci (match exact du nom de fichier, le kebab « Actions for … » partageant le mot). **Gates verts** : API typecheck + **136 tests** (+10) ; web typecheck, lint **0 erreur**, **47 tests**, build OK. **Différé** : sélection de destination = dossiers existants (pas de création de dossier à la volée depuis le menu) ; pas de multi-sélection ; drag tactile non couvert (le menu fournit la voie clavier/tactile). |
| 2026-06-23 | Feature (Éditeur projet : mode Easy / Advanced — formulaire OpenFOAM piloté par catalogue) | `apps/web/src/features/projects/{foamFieldCatalog.ts(new),foamModel.ts(new),foamModel.test.ts(new),foamForm.ts(new),foamForm.test.ts(new),CaseFileForm.tsx(new),CaseFileForm.test.tsx(new)}`, `apps/web/src/features/files/FileTreeEditor.tsx`, `apps/web/src/pages/ProjectEditPage.test.tsx` (déjà ajusté), `apps/web/src/pages/ProjectEditPage.tsx` | Demande utilisateur : pour l'édition des fichiers **dans la zone projet**, un **Easy mode** (inputs + listes déroulantes par champ, « cliquer le bon choix ») à côté de l'**Advanced mode** (l'éditeur brut CodeMirror existant). **Recherche déléguée à un sous-agent** (comme demandé) digérant `docs/DOCUMENTATION.md` (réf. OpenFOAM v2412) → **catalogue de champs typé** `foamFieldCatalog.ts` (13 fichiers : U/p/k/omega/nut/epsilon/nuTilda, transportProperties, turbulenceProperties, controlDict, decomposeParDict, fvSchemes, fvSolution ; 72 champs, **347 tokens d'options** dont 164 types de C.L. par champ 0/, listés exactement). **Round-trip sûr** : `foamModel.ts` = parser **position-aware** (tokenizer tolérant — saute commentaires, chaînes, blocs `#{ … #}` ; spans de valeur) → `setFoamValue(path,val)` **splice** uniquement la valeur ciblée (bannière/commentaires/macros/inconnu **préservés octet-pour-octet**, jamais de ré-sérialisation). `CaseFileForm.tsx` : rend chaque feuille en **`<select>`** (enum/bool : types de C.L., schémas, solveurs, modèles, on/off) ou **input texte** (scalaire/vecteur/dimensions/inconnu, commit **au blur** pour ne pas re-parser à chaque frappe) ; `boundaryField` → un select **par patch** ; sous-dictionnaires connus (RAS/SIMPLE/solvers/ddtSchemes…) en sections ; blocs complexes/inconnus (`functions`) → renvoi explicite vers Advanced. Le formulaire écrit dans le même `draft` → **autosave existant**. **Toggle Easy/Advanced** (`ModeToggle`, `aria-pressed`) dans l'en-tête de l'éditeur, **par fichier**, défaut **Easy** si fichier reconnu par le catalogue sinon Advanced ; activé **uniquement côté projet** (`enableEasyMode`, templates inchangés) ; fichiers non-dictionnaire (ex. polyMesh/boundary, format liste) → pas de toggle. **Séquence skills `CLAUDE.md §0`** : cadrage `ui-ux-pro-max` + revue `web-design-guidelines` (labels `htmlFor`/`id` **uniques par chemin**, `<select>` `bg-surface`/`text-text` explicites, inputs **non contrôlés** au blur, `spellCheck=false`/`translate=no` sur valeurs brutes, `focus-visible`, `overflow-x-hidden`+`overscroll-contain`, `<button>`+`aria-pressed` sémantique). **Tests** : +22 web (`foamModel` 12 : parse/get/set surgical/nested/insert ; `foamForm` 5 : match catalogue + disponibilité ; `CaseFileForm` 5 : select enum splice, type par patch isolé, commit au blur, hint Advanced) ; `ProjectEditPage.test` durci (match exact du nom de fichier). **100 % frontend**, aucun changement backend. **Gates verts** : typecheck, lint **0 erreur** (aucun warning sur les nouveaux fichiers), **69 tests web** (+22), build OK. **Différé** : options de schémas = presets (un ψ différent passe par Advanced) ; édition de champs **présents** uniquement (insertion de champ absent existe via `insertFoamField` mais non câblée à l'UI) ; pas de swap auto des paramètres au changement de type de C.L. ; non appliqué aux templates. |
| 2026-06-23 | Feature (Conversion CGNS → OpenFOAM : ajout fichier CGNS + « Convert to Foam ») | **Shared** : `packages/shared/src/index.ts` (codes `INVALID_CGNS`/`CONVERSION_FAILED`, const `CGNS_EXTENSION`, `CONVERSION_STEPS`/`ConversionStepId`). **API** : `apps/api/src/config/env.ts` (toolchain : `PVPYTHON_BIN`, `CGNS_TO_VTK_SCRIPT`, `VTK_TO_FOAM_BIN`, `CHECK_MESH_BIN`, `OPENFOAM_BASHRC`, `CONVERSION_STEP_TIMEOUT_MS`), `apps/api/.env.example`, `apps/api/src/lib/{commandRunner.ts(new),cgnsStorage.ts(new),caseStorage.ts(+`caseDirAbsolute`)}`, `apps/api/src/modules/projects/{conversion.service.ts(new),conversion.controller.ts(new),conversion.schemas.ts(new),projects.routes.ts}`, `apps/api/tests/conversion.test.ts(new)`. **Web** : `apps/web/src/lib/api/{types.ts,conversion.ts(new)}`, `apps/web/src/features/projects/{useConversion.ts(new),MeshConversionSection.tsx(new),ConvertToFoamFlow.tsx(new),MeshConversionSection.test.tsx(new)}`, `apps/web/src/pages/ProjectDetailPage.tsx`. Demande utilisateur : après l'import polyMesh + template, pouvoir **ajouter un fichier CGNS** ; ce n'est **qu'une fois un CGNS ajouté** que se **débloque** un bouton **Convert to Foam** → choisir un template → **confirmation** « êtes-vous sûr ? » → lancer `pvpython CgnsToVtk.py <cgns>` (→ VTK) puis `vtkUnstructuredToFoam <vtk>` (→ `constant/polyMesh`) puis `checkMesh`. Cible de déploiement **Debian** (dev Windows sans OpenFOAM) → **« fait comme si Debian »**. **Backend** : CGNS stockés **à part du cas** sous `STORAGE_DIR/projects/<id>/cgns/` (façade `cgnsStorage` sur le socle path-safe `fileTreeStorage`, basename aplati, validation extension `.cgns`) ; `commandRunner` = wrapper `execFile` **injectable** (`setCommandRunner`) qui **ne jette jamais** (sortie ≠ 0, timeout, **ENOENT binaire absent** → résultat structuré) ; `conversion.service.convertCgnsToFoam` orchestre : assert visibilité → **applique le template** choisi (ajoute, garde l'existant) → **garantit `system/controlDict`** (scaffold minimal si le template l'omet, sinon les utilitaires OF échouent) → exécute les **3 étapes en séquence, court-circuit à la 1ʳᵉ échouée** (chaque étape : argv réel — jamais de shell — cwd, timeout `CONVERSION_STEP_TIMEOUT_MS` 10 min, `maxBuffer` 16 Mo ; logs **tronqués en queue** 20 k) → renvoie `{ success, steps[], notes[], verification, entries }` (échec de pipeline = **200** avec `success:false`, pas une exception → l'UI montre les logs ; seules les erreurs de validation jettent 404/422). Tools **configurables** (défauts Linux PATH) ; utilitaires OpenFOAM enveloppés `bash -c 'source "$OPENFOAM_BASHRC" && exec "$@"'` **seulement** si `OPENFOAM_BASHRC` défini, args passés en `"$@"` (**injection-safe**), sinon exécutés directs ; `CGNS_TO_VTK_SCRIPT` défaut = `python/CgnsToVtk.py` du repo (dev), à fixer en prod. Endpoints (auth + visibilité projet, membres inclus) : `GET/POST/DELETE /projects/:id/cgns`, `POST /projects/:id/cgns/convert`. **Frontend** (séquence skills `CLAUDE.md §0` suivie : `ui-ux-pro-max → impeccable (substitut frontend-design) → design-taste-frontend → web-design-guidelines`) : **section « Mesh conversion »** propre (sa propre zone sous Case files) sur `/projects/:id` — **un CTA orange par état** (vide : « Add CGNS file » orange + « Convert to Foam » **verrouillé** = `span role=button aria-disabled` focusable + **tooltip** « Add a CGNS file first » + icône cadenas ; avec CGNS : « Convert to Foam » orange déverrouillé, « Add » secondaire) ; liste CGNS (mono, taille `tabular-nums`, retrait ghost danger). **Flow `ConvertToFoamFlow`** (Dialog Radix) : picker template (réutilise `useTemplatesQuery`) → **confirm** (sélecteur CGNS si plusieurs, rappel « écrase `constant/polyMesh`, irréversible », **aperçu du pipeline** numéroté) → **run** : état *running* (`role=status aria-live`, spinner — pas de fausse progression live car la conversion est **un seul POST**) puis **rapport** = **stepper de pipeline** (rail de statut vertical reliant des nœuds **numérotés** — séquence réelle, pas « Step N » générique anti-slop ; statut **icône+texte+couleur** OK/Failed/Skipped ; exit code + durée `tabular-nums`) avec **logs stdout/stderr repliables** (disclosure `aria-expanded`, région scrollable focusable `max-h-48 overscroll-contain`, ouverte par défaut pour l'étape échouée + le **checkMesh**), bannière `role=status`/`role=alert`, notes (template appliqué, base générée), liens Close / Open files. Tokens seuls, AA, pas de side-stripe, pas de carte imbriquée, **zéro em-dash** dans la copie. **Cross-platform** (l'app finit sur Linux) : chemins logiques forward-slash, `path` natif, `pvpython.exe` accessible via `PVPYTHON_BIN`. **Tests** : +14 API (`conversion` : upload `.cgns` ok/extension invalide `INVALID_CGNS`/404 étranger ; delete ; **runner factice injecté** simulant le toolchain — happy path écrit le mesh + `hasMesh` + ordre des 3 étapes ; scaffold quand template sans controlDict ; **court-circuit** échec étape 1 ; **binaire absent** ENOENT → étape failed ; 404 CGNS/template absent ; 422 ; super-admin) + 3 web (CTA **verrouillé** tant que pas de CGNS ; liste + déverrouillage ; flow pick→confirm→rapport avec sortie checkMesh). **Gates verts** : `build:shared`, lint **0 erreur** (aucun warning sur les nouveaux fichiers), typecheck, **222 tests** (150 API + 72 web), build (chunk `ProjectDetailPage` ~48,5 ko / 11,5 ko gzip). **Différé** : conversion **synchrone** (pas de file de jobs / progression live par étape — cohérent avec l'absence de modèle `Run` ; à migrer en tâche de fond quand le serveur distant existera) ; pas d'aperçu du mesh converti. |
| 2026-06-23 | Tweak UI (page détail : scroll épinglé + Mesh conversion relocalisée en dialog + overlay élargi) | `apps/web/src/components/layout/AppShell.tsx`, `apps/web/src/pages/ProjectDetailPage.tsx`, `apps/web/src/features/projects/{CaseFilesSection,CaseSummary,ConvertToFoamFlow}.tsx`, `apps/web/src/features/projects/ConvertToFoamFlow.test.tsx` (new) ; **suppr.** `apps/web/src/features/projects/MeshConversionSection{,.test}.tsx` | Trois retours utilisateur sur `/projects/:id`, **100 % frontend** (backend/API/hooks **intacts**). Séquence skills `CLAUDE.md §0` : `ui-ux-pro-max` (cadrage) → `redesign-existing-projects` (écrans existants) → `design-taste-frontend` (anti-slop ; *design read* = **UI produit/assistant multi-étapes, pas landing** → seules règles universelles appliquées : **zéro em-dash**, 1 accent orange, AA, états loading/empty/error/hover/focus/disabled, divulgation progressive) → `web-design-guidelines` (review). `frontend-design` non installé → substitué (cf. précédents). **(1) Page non-scrollable** : page détail **épinglée au viewport dès `lg`** (`AppShell` route détail → `lg:flex lg:h-[calc(100dvh-4rem)] lg:flex-col lg:overflow-hidden`, comme l'éditeur ; **sous `lg` = flux naturel** mobile) + chaîne flex `lg:` (page → wrapper → carte **Case files** `lg:min-h-0 lg:flex-1` → `Tabs` → `TabsContent` → arbre/`CaseSummary` `lg:flex-1`, toolbar Files `lg:shrink-0`) pour que **seul l'onglet actif scrolle** en interne (`overscroll-contain`), jamais la page. **Cause racine du « 2 panneaux superposés » (repérée via DevTools par l'utilisateur)** : Radix garde l'onglet **inactif monté avec `hidden`** ; un `lg:flex` brut (CSS auteur) **écrase le `[hidden]` du navigateur** → le panneau inactif réapparaissait. Corrigé en **gatant le flex à l'état actif** : `lg:data-[state=active]:flex` (vérifié dans le CSS de prod ; variant déjà utilisé dans `tabs.tsx`). États **vides/erreur centrés** dans la carte remplie (`lg:my-auto`, fix du « Summary décalé » signalé en run). **Piège évité** : `min-h-0 flex-1` à *toutes* les tailles effondrait la carte sous `lg` (parent hauteur auto + `flex-basis:0`) → tout le *fill* est **gated `lg:`**. **(2) Mesh conversion relocalisée** : suppression de la **carte autonome** `MeshConversionSection` (jugée inutile là) ; son `ConvertToFoamFlow` devient un **dialog autonome** qui **possède son propre roster CGNS** (`useCgnsFilesQuery`) et gagne une **étape « Sources »** (upload/list/remove CGNS, **« Continue » désactivé tant qu'aucun `.cgns`**) avant picker → confirm → run. Lancé par un bouton **secondaire « Convert a CGNS mesh »** (ghost bleu) dans l'empty state **« No case files yet »** (le CTA orange y reste **« Import folder »** — règle scalpel) **et** **« Convert mesh »** (secondaire) dans la toolbar Files (conversion atteignable même après import). **(3) Overlay élargi** : `DialogContent` du flux `max-w-[480px]` → **`max-w-2xl` sur toutes les étapes** (largeur **constante** = pas de resize entre étapes ; **rapport de pipeline + logs** sans scroll). **Tests** : `MeshConversionSection.test` retiré → **`ConvertToFoamFlow.test`** (3 : « Continue » verrouillé tant que 0 CGNS ; liste + déverrouillage ; flow **sources → pick → confirm → rapport** checkMesh). **Gates verts** : typecheck, lint **0 erreur**, **72 tests web** (test réécrit), build (chunk `ProjectDetailPage` ~48,9 ko / 11,8 ko gzip). **Différé** : épinglage non appliqué à d'autres écrans (hors périmètre). |
| 2026-06-24 | Feature (Onglet « Visualize » : viewer 3D du maillage OpenFOAM — `3D_PLAN.md`) | **Shared** : `packages/shared/src/index.ts` (`MeshPatch`/`MeshManifest`, `VIZ_DIRNAME`, codes `NO_MESH`/`MESH_NOT_BUILT`/`MESH_BUILD_FAILED`/`SCRIPT_MISSING`). **API** : `apps/api/scripts/{extractPatches.py(new),requirements.txt(new)}`, `apps/api/tests/fixtures/extractPatches.py(new)`, `apps/api/src/config/env.ts` (`MESH_PYTHON_BIN` défaut **plateforme** : `python` Win / `python3` Debian, `EXTRACT_PATCHES_SCRIPT`, `MESH_BUILD_TIMEOUT_MS`), `apps/api/.env.example`, `apps/api/vitest.config.ts`, `apps/api/src/lib/vizStorage.ts(new)`, `apps/api/src/modules/projects/{mesh.service.ts(new),mesh.controller.ts(new),projects.routes.ts}`, `apps/api/tests/mesh.test.ts(new)`. **Web** : `apps/web/package.json` (+`three`/`@types/three`), `apps/web/vite.config.ts` (chunk `three`), `apps/web/src/lib/api/{types,projects}.ts`, `apps/web/src/features/visualize/{useMesh.ts,PatchTable.tsx,MeshViewer.tsx,PatchTable.test.tsx}(new)`, `apps/web/src/pages/{ProjectDetailPage.tsx,ProjectDetailPage.test.tsx(new)}`. Portage du `patch_viewer.py` (PyVista/Trame, sibling hors-repo, commentaires allemands) **dans l'app** sous forme d'onglet **Visualize** sur `/projects/:id`, **optimisé + traduit en anglais**. **Décision d'archi (option C du plan)** : pas de serveur Trame persistant ni de parsing polyMesh en JS ; un **script Python one-shot** (réutilisant le précédent `commandRunner`/`CgnsToVtk.py`) extrait **uniquement les surfaces de bord** → **GLB compact + manifest JSON**, mis en cache disque ; three.js rend côté navigateur. **Parité fonctionnelle** F1–F9 : F1/F2 (lecture patches + types via regex `boundary`) dans `extractPatches.py` (lit le bloc `boundary`, saute `internalMesh` → gain mémoire ; `nFaces` = compte **avant** triangulation pour coller à la table d'origine) ; F3 surfaces grises **+ arêtes** (`EdgesGeometry` seuillé 20°) ; F4 table Name/Type/nFaces ; F5 clic → patch **orange**, autres atténués (opacity 0.12) ; F6 « Show all » ; F7 orbit/zoom/pan + **Reset view** (fit bounding-sphere) ; F8 WebGL three.js ; F9 layout déjà normalisé par le storage (n/a) ; **+ pick 3D** (raycaster, clic surface → sélectionne le patch, seuil anti-drag). **Backend** : `vizStorage` (artefacts sous `…/projects/<id>/viz/`, **sibling du cas** → un reset n'efface pas le rendu ; staleness par mtime GLB vs `boundary`/`points`) ; `mesh.service` calque `conversion.service` (visibilité projet → **404** étranger ; gate `hasPolyMesh` → **409 NO_MESH** ; build-on-demand **synchrone** borné par `MESH_BUILD_TIMEOUT_MS` ; sortie absente = **502 MESH_BUILD_FAILED** ; `SCRIPT_MISSING` si script absent) ; `GET …/mesh/manifest` (build au besoin), `GET …/mesh/geometry` (Buffer GLB, `model/gltf-binary`, `Cache-Control private`), `POST …/mesh/rebuild`. **Frontend** (séquence skills `CLAUDE.md §0` : `ui-ux-pro-max → impeccable (substitut frontend-design) → design-taste-frontend → web-design-guidelines`) : `three` **code-splittée** (`manualChunks`) + **lazy** `MeshViewer` (jamais sur le bundle initial — vérifié : chunks `three` ~575 ko / `MeshViewer` ~11,7 ko séparés) ; hooks `useMesh` (manifest déclenche le build → état « Building 3D preview » ; geometry `ArrayBuffer` gated sur succès ; `staleTime` généreux) ; **`MeshViewer`** scène three.js vanilla (couleurs **lues sur les tokens** via `getComputedStyle` — `--color-accent`/`--color-neutral`/`--color-text-secondary`, **scène claire studio** = radial token + canvas WebGL transparent, **pas de canvas sombre**), highlight = swap matériau, `OrbitControls` damping **gated `prefers-reduced-motion`**, `ResizeObserver`, **cleanup** complet (dispose géométries/matériaux/renderer, listeners, RAF) ; tous les états : **WebGL indisponible** (détection), **building** (spinner + skeleton), **error** (panneau `role=alert` danger + détail repliable + Retry=rebuild), **empty** (losange), **loading geometry** ; `PatchTable` (primitive `table`, lignes **activables clavier** Enter/Espace, `aria-selected`, sélection = **fond `accent-tint` + nom semibold** — **pas de side-stripe**, orange jamais en texte = AA, `nFaces` `tabular-nums` aligné droite, `scrollIntoView` gardé pour jsdom) ; **strip d'onglets** `Detail | Visualize` entre `PageHeader` et le corps (Radix `Tabs`), **Visualize désactivé** tant qu'aucun `constant/polyMesh/` (tooltip « Import a polyMesh to enable 3D » via wrapper `span` focusable), ouvrir Visualize **remplace** le détail plein-cadre (`<Suspense>`), **pas de changement de route ni de regex `AppShell`**. Anti-slop (*design read* = **UI produit/outil, pas landing**) : zéro em-dash, 1 accent orange, pas de carte imbriquée (un panneau divisé par hairline), lucide conservé (dépendance projet), pas de point d'état décoratif. **Tests** : +10 API (`mesh` : 401 ; **409 NO_MESH** sans polyMesh ; build + manifest ; **réutilisation cache** = pas de 2ᵉ run ; **502** échec extracteur ; **404** étranger ; geometry `model/gltf-binary` + octets ; **409 MESH_NOT_BUILT** ; rebuild force le run ; super-admin — runner factice écrit GLB+manifest) + 8 web (`PatchTable` 5 : rendu/sélection/toggle/`aria-selected`/« Show all » ; `ProjectDetailPage` 3 : Visualize désactivé sans mesh / activé avec / ouvre le viewer & masque le détail — `MeshViewer`+`CaseFilesSection` mockés, `userEvent` pour l'activation Radix). **Gates verts** : `build:shared`, **API typecheck + 160 tests** (+10), **web typecheck + lint 0 erreur + 80 tests** (+8) + **build OK** (chunks `three`/`MeshViewer` lazy). Dép. : `npm i -w @dive/web three` (+ `@types/three`). **Note environnement** : le viewer exige **Python 3 + `pyvista`/`trimesh`/`numpy`** (`apps/api/scripts/requirements.txt`) sur l'hôte API ; sur la cible **Debian** `python3`/PyVista existent déjà (comme `pvpython`/CGNS). Sur un poste **Windows** sans Python sur le PATH, le build échoue (`9009` / alias Microsoft Store) — état d'erreur **géré proprement** (panneau + Retry, le reste de la page intact) ; régler `MESH_PYTHON_BIN` ou installer Python+deps. **Différé (cf. §11)** : build **synchrone** (pas de file de jobs / progression — cohérent avec l'absence de modèle `Run`) ; coloration par champ (pression/vitesse) à l'arrivée du solveur ; toggle de visibilité par patch ; deep-link `?tab=visualize`. |
| 2026-06-24 | Fix (onglets Detail/Visualize : panneau inactif visible en grand écran) | `apps/web/src/pages/ProjectDetailPage.tsx` | Régression signalée en run (capture DevTools) : sur écran large, la carte **Case files** était écrasée à mi-hauteur et l'arbre de fichiers disparaissait. Cause = même piège que §211 : j'avais mis un `flex`/`lg:flex` **inconditionnel** sur les deux `TabsContent` (Detail et Visualize), ce qui **écrase le `[hidden]`** de Radix sur l'onglet **inactif** → le panneau Visualize (vide) s'affichait en **2ᵉ frère `flex-1`** et volait la moitié de la hauteur. **Correctif** : flex **gaté à l'état actif** (`data-[state=active]:flex` au lieu de `flex` brut ; pattern éprouvé `tabs.tsx`/§211), pour les deux panneaux. **Bonus** : `MeshViewer` n'est **monté que si l'onglet Visualize est ouvert** (`{view === 'visualize' && …}`) → le build serveur (Python) n'est **jamais déclenché** depuis l'onglet Detail. **Gates verts** : web typecheck + `ProjectDetailPage.test` (3) verts. |
| 2026-06-24 | Fix (viewer 3D : canvas blanc, rien ne s'affiche) | `apps/web/src/features/visualize/MeshViewer.tsx` | Build OK mais **canvas blanc** signalé en run. Deux causes dans la scène three.js : **(1)** je **reparentais chaque mesh** dans un `Group` neuf **pendant** `gltf.scene.traverse()` → ça **mute l'arbre en cours de parcours** ET **supprime les transforms des nœuds ancêtres** du GLB (trimesh en pose) → géométrie déplacée hors-champ. Corrigé : collecte des meshes **sans muter**, puis `root.add(gltf.scene)` **entière** (transforms préservés) ; `raycaster.intersectObjects(root.children, true)` (récursif, meshes désormais imbriqués). **(2)** trimesh exporte **sans normales** → `MeshStandardMaterial` non éclairé (quasi invisible) ; ajout de `computeVertexNormals()` si l'attribut manque. **(3)** l'erreur de parse n'est plus **avalée en silence** : nouvel état `failed` → **panneau d'erreur + Retry** (rebuild) au lieu d'un blanc muet (idem si 0 mesh). **Gates verts** : web typecheck, lint **0 erreur**, build OK (chunks `three`/`MeshViewer` lazy inchangés). |
| 2026-06-24 | Perf (viewer 3D : fluidité) | `apps/web/src/features/visualize/MeshViewer.tsx` | Retour utilisateur (capture de l'outil VTK d'origine) : « beaucoup plus fluide qu'actuellement, optimise ». Trois tueurs de fluidité corrigés. **(1) Rendu à la demande** : suppression de la boucle `requestAnimationFrame` permanente (qui redessinait une scène statique à 60 fps) → on ne rend **que** sur changement (event `change` d'`OrbitControls` — input + inertie damping, `resize`, highlight, toggle, fit). **Idle = 0 GPU** ; l'inertie reste lisse car chaque `change` programme exactement une frame. **(2) Plus de raycast au survol** : le `pointermove` lançait un `raycaster.intersectObjects(…, true)` **plein-maillage à chaque mouvement de souris** (O(n) sans BVH) — principale cause du lag. Supprimé ; le raycast ne se fait plus **qu'au clic** (sélection). Curseur `grab`/`grabbing` géré en CSS. **(3) Matériau moins cher** : `MeshStandardMaterial` (PBR, métal/rugosité) → **`MeshLambertMaterial`** (diffus une passe), coût par pixel bien moindre pour une surface grise technique. **Bonus look + perf** : arêtes (F3) passées en seuil **1°** (garde les vraies arêtes de cellules, retire les diagonales de triangulation coplanaires → ressemble à la **grille quad** de l'outil d'origine, pas à un fil-de-fer de triangles) ; **toggle Edges** (bouton `Grid3x3`, `aria-pressed`) pour les masquer/afficher (visibilité seule, pas de rebuild) ; **caps par nombre de triangles** : > 400 k arêtes **cachées par défaut** (orbit fluide, toggle pour voir), > 2 M arêtes **pas construites** (toggle masqué). `powerPreference:'high-performance'`. Thème **clair conservé** (mandat charte ; l'aperçu sombre de l'outil d'origine n'est pas repris sauf demande explicite). **Gates verts** : web typecheck, lint **0 erreur**, **80 tests** verts, build OK (chunk `MeshViewer` ~13 ko / `three` lazy inchangé). |
| 2026-06-24 | Tweak UI (détails projet → menu engrenage, gain de place) | `apps/web/src/pages/ProjectDetailPage.tsx` | Demande utilisateur : la **carte « Details »** (Owner / Created) prenait de la hauteur sur **tous** les onglets. Déplacée dans le **menu engrenage** (pattern déjà établi §196 : `overflow-menu`/`progressive-disclosure`). Nouvel item **« Project details »** (icône `Info`) → **Dialog** lecture-seule (`dl` owner+created, même contenu). L'engrenage est désormais **visible par tous les membres** (avant : owner/super-admin seulement) avec « Project details » pour tous ; **« Manage collaborators »** + **« Delete project »** (destructif) restent **gatés `canManage`** (séparés par diviseurs). Le corps de l'onglet **Detail** ne contient plus que **Case files** → pleine hauteur partout. `aria-label` du déclencheur « Project settings » → « Project menu ». **Gates verts** : web typecheck, lint **0 erreur**, `ProjectDetailPage.test` (3) verts. |
| 2026-06-24 | Feature (Viewer 3D : renommer un patch de bord) | **Shared** : `packages/shared/src/index.ts` (code `PATCH_EXISTS`). **API** : `apps/api/src/lib/openfoamCase.ts` (`isValidPatchName`, `renameBoundaryPatch`, `renameFieldBoundaryPatch` + `boundaryFieldSpan`), `apps/api/src/modules/projects/{mesh.service,mesh.controller,mesh.schemas(new),projects.routes}.ts`, `apps/api/tests/mesh.test.ts`. **Web** : `apps/web/src/lib/api/projects.ts`, `apps/web/src/features/visualize/{useMesh.ts,PatchTable.tsx,PatchTable.test.tsx,RenamePatchDialog.tsx(new),MeshViewer.tsx}`. Demande utilisateur : pouvoir **changer le nom des boundary** depuis le 3D (objectif d'origine de `patch_viewer` = contrôler le nommage des patches). **Backend** : renommage **correct** (garde le cas valide) — (1) entête du patch dans `constant/polyMesh/boundary` via splice ciblé (regex entête `nom\s*\{` : ne touche **ni** une valeur `type nom;` **ni** un groupe `1(nom)`), (2) la clé `nom { … }` dans le bloc `boundaryField` de **chaque fichier de champ** (scan borné : fichiers texte ≤ 2 Mo hors `constant/polyMesh/` contenant `boundaryField`, splice **dans le span boundaryField uniquement** → laisse intact `functions`/commentaires). `POST /projects/:id/mesh/patches/rename` `{from,to}` — validation zod (`to` = mot OpenFOAM `^[A-Za-z_]\w*$` → 422 sinon), `404` patch absent, **409 `PATCH_EXISTS`** collision, `409 NO_MESH` sans boundary, visibilité projet (404 étranger). Le fichier `boundary` change → rendu **stale** → reconstruit au prochain manifest. **Frontend** : bouton **crayon par ligne** dans `PatchTable` (`aria-label="Rename …"`, `stopPropagation` cellule → ne sélectionne pas la ligne), **`RenamePatchDialog`** (react-hook-form + zod : mot valide, pas de collision client + serveur ; `INVALID`/`PATCH_EXISTS` mappés sur le champ ; toast succès), hook `useRenamePatch` qui **droppe manifest + geometry** (refetch ordonné : manifest reconstruit le GLB d'abord, geometry gated dessus → pas de course GLB périmé) + invalide l'arbre `files` (le nom vit dans un fichier de cas). La **sélection** (et son surlignage 3D) **suit** le patch renommé. **Tests** : +6 API (`rename` : boundary + boundaryField des champs renommés / autres patches & `internalField` intacts ; 409 collision ; 404 absent ; 422 nom invalide ; 409 NO_MESH ; 404 étranger) + 2 web (`PatchTable` : action crayon n'ouvre pas la sélection ; cachée sans `onRename`). **Gates verts** : `build:shared`, **API typecheck + 166 tests** (+6), **web typecheck + lint 0 erreur + 82 tests** (+2), build OK (chunk `MeshViewer` ~15,8 ko). **Différé** : pas de renommage du **type** de patch (patch/wall/…) depuis le 3D (séparé) ; reconstruction synchrone du rendu après renommage (cohérent avec le reste). |
| 2026-06-24 | Fix/Amélioration (Viewer 3D : vraies arêtes de cellules + z-fighting) | **API** : `apps/api/scripts/extractPatches.py`, `apps/api/src/lib/vizStorage.ts`, `apps/api/src/modules/projects/{mesh.service,mesh.controller,projects.routes}.ts`, `apps/api/tests/mesh.test.ts`. **Shared** : `packages/shared/src/index.ts` (`MeshPatch.edgeOffset?`/`edgeCount?`). **Web** : `apps/web/src/lib/api/projects.ts`, `apps/web/src/features/visualize/{useMesh.ts,MeshViewer.tsx}`. Retour utilisateur : « l'affichage des *mesh* sur la forme 3D n'est pas correct » (capture de l'outil VTK d'origine = grille de cellules nette). **Cause** : je triangulais la surface pour le GLB → la **grille quad d'origine était perdue** ; `EdgesGeometry` côté client montrait soit les **diagonales de triangulation** (surfaces courbes), soit **rien** sur les zones planes (arêtes coplanaires masquées) → ne ressemblait pas au maillage réel. **Correctif** : extraction des **vraies arêtes de cellules** côté Python **avant triangulation** (`surf.extract_all_edges()` — la grille poly/quad réelle, sans diagonales), écrites en **`edges.bin`** (float32 brut, endpoints de segments) à côté du GLB, avec **offset/longueur par patch** dans le manifest (`edgeOffset`/`edgeCount`, en sommets). **Défensif** : échec d'extraction d'arêtes ⇒ `edges.bin` vide / absent, jamais d'échec du build (le viewer retombe sur l'overlay client). **Backend** : `vizStorage` (chemin `edges.bin`, `readVizEdges`, **`vizIsStale` vrai si `edges.bin` manque** → les rendus déjà en cache se régénèrent une fois pour produire les arêtes), `getMeshEdges` + `GET /projects/:id/mesh/edges` (octet-stream, 404 si absent → fallback). **Frontend** : `useMeshEdgesQuery` (gated comme la geometry ; rebuild/rename droppent aussi le cache arêtes), `MeshScene` construit les `LineSegments` **depuis la tranche `edges.bin`** du patch (coords brutes = même espace local que le mesh GLB → ajoutées **en enfant du mesh** → héritent du transform du nœud → alignées au pixel), **fallback `EdgesGeometry(1°)`** si pas d'arêtes ; rendu attend que geometry **et** edges soient résolus (une seule construction). **Z-fighting corrigé** : `polygonOffset` sur le matériau de surface → les lignes ne « cassent » plus / ne clignotent plus. Les **caps** de fluidité (toggle Edges, `EDGE_SHOW_CAP`) conservés. **Tests** : +2 API (`mesh/edges` : flux octet-stream après build ; 404 si non construit) ; le faux runner écrit aussi `edges.bin` (sinon `vizIsStale` reconstruirait en boucle). **Gates verts** : `build:shared`, **API typecheck + 168 tests** (+2), **web typecheck + lint 0 erreur + 82 tests** + build OK (chunk `MeshViewer` ~16,6 ko). **Note** : nécessite la version à jour de `extractPatches.py` (déjà dans le repo) ; les projets déjà rendus se reconstruisent automatiquement au prochain affichage (cache marqué stale). |
| 2026-06-24 | Feature (Auto-patch des boundaries : `autoPatch <featureAngle> -overwrite`) | **Skills** : `ui-ux-pro-max` → `impeccable` (substitut de `frontend-design`, non installé) → `design-taste-frontend`, review finale `web-design-guidelines`. **API** : `apps/api/src/lib/openfoamCommand.ts` (nouveau — `planOpenfoamCommand` + `commandFailed` **extraits** de `conversion.service` pour être partagés ; sourcing `OPENFOAM_BASHRC`, argv réel = injection-safe), `conversion.service.ts` (importe le helper, copies locales supprimées), `config/env.ts` + `.env.example` (`AUTO_PATCH_BIN`, défaut `autoPatch`), `mesh.schemas.ts` (`autoPatchSchema` : `featureAngle` `z.coerce.number 0..180 .default(45)` → 422 hors-bornes), `mesh.service.ts` (`autoPatchMesh` : assert visibilité → **gate 409 NO_MESH** → scaffold `system/controlDict` si absent → `autoPatch <angle> -overwrite -case <dir>` via runner injectable, timeout `CONVERSION_STEP_TIMEOUT_MS` ; échec outil **ne throw pas** → `success:false` + logs (comme la conversion) ; relit `boundary` pour renvoyer les patches), `mesh.controller.ts` (`autoPatchController`), `projects.routes.ts` (`POST /:id/mesh/auto-patch`), `tests/mesh.test.ts` (+7 : succès angle défaut 45, angle custom, échec outil = `success:false`+logs, 401/404/409 NO_MESH/422 hors-bornes ; faux runner réécrit le `boundary` comme `-overwrite`). **Web** : `lib/api/types.ts` (`AutoPatchResult`/`AutoPatchResponse`), `lib/api/projects.ts` (`autoPatchMesh`), `features/visualize/useMesh.ts` (`useAutoPatch` : au succès **droppe** manifest/geometry/edges + invalide `files`, exactement comme `useRenamePatch` ; le `boundary` réécrit rend `vizIsStale` vrai → le viewer **reconstruit** au prochain fetch, pas de rebuild dans le service), `features/visualize/AutoPatchDialog.tsx` (nouveau — `Field` numérique « Feature angle (degrees) » défaut 45 + helper, note `accent-tint` « réécrit `constant/polyMesh/boundary`, les `boundaryField` peuvent devoir être ajustés », panneau d'échec `border-danger` avec commande+exit code+stderr, `role="alert"` **restreint au titre** pour ne pas faire lire tout le stderr au lecteur d'écran, `Cancel` secondaire + **CTA orange unique** `Run autoPatch`, états loading/disabled), `features/visualize/MeshViewer.tsx` (bouton secondaire bleu `Auto-patch` + icône `Scissors` dans l'en-tête du panneau des patches ; rend le dialog ; vide la sélection `onPatched`). **Conformité DESIGN.md** : un seul CTA orange par zone, tokens uniquement, lucide (1 famille), label-above/erreur-below via `Field`, contrastes AA (CTA `#A85F00` 4.88:1, danger). **Gates vertes** : **API typecheck + 175 tests** (+7), **web typecheck + lint 0 erreur + 82 tests** + build OK (chunk `MeshViewer` ~20,3 ko, inclut le dialog). **Note** : `autoPatch` doit être sur le PATH OpenFOAM du serveur Debian (sinon échec lisible `success:false`, pas de crash) ; sur un import maillage-seul, les fichiers de base sont scaffoldés pour que l'utilitaire tourne. |
| 2026-06-24 | Feature (Solveur — Slice 0 : cas runnable simpleFoam + gate) | Plan dédié **`SOLVER_PLAN.md`** (synthèse 5 agents). **Shared** : codes `NOT_RUNNABLE`/`RUN_IN_PROGRESS`/`RUN_NOT_FOUND`, `RUN_DIRNAME`, `RUN_STATUSES`/`RunStatus`/`ACTIVE_RUN_STATUSES`/`isTerminalRunStatus`, `SOLVER_IDS`/`SolverId`, `RESIDUAL_FIELDS`/`ResidualField`/`ResidualSample`. **API** : `lib/openfoamCase.ts` (`SOLVER_FILE_PATHS` + `renderSolverFile` — `system/{controlDict application simpleFoam, fvSchemes div réels + wallDist meshWave, fvSolution GAMG+smoothSolver + residualControl + relaxation + pRefCell}`, `constant/{transportProperties Newtonian nu, turbulenceProperties RAS kOmegaSST}`, `0/{U,p,k,omega,nut}` avec boundaryField par patch ; `parseApplication`), `modules/projects/files.service.ts` (`computeRunnable`/`verifyRunnable` = gate mesh + fichiers solveur présents + `application` ; `scaffoldSolver` génère les manquants **sans écraser**), `files.controller`/`projects.routes` (`GET /:id/runnable`, `POST /:id/runnable/scaffold`). **Découverte clé (vérifiée)** : `scaffoldCase` ne produit que le trio `system` + `0/{U,p}` + `application foamRun` → **un cas scaffoldé n'est PAS exécutable par simpleFoam** (manquent `transport/turbulenceProperties`, `0/{k,omega,nut}`) ; `scaffoldSolver` réutilise la mécanique de scaffold pour le rendre runnable plutôt qu'un nouveau système. **Tests** : +9 API (`runnable` : render unité, gate polyMesh-seul liste les manquants, scaffold idempotent → runnable + `solver:simpleFoam`, mesh absent, 404 étranger). **Gates verts** : `build:shared`, API typecheck. |
| 2026-06-24 | Feature (Solveur — Slice 1 : modèle `Run` + exécution en tâche de fond + réconciliation) | **API** : `config/env.ts`+`.env.example` (`SOLVER_BIN`, `SOLVER_MAX_RUNTIME_MS` 6 h, `SOLVER_MAX_CONCURRENT_RUNS` 1, `SOLVER_LOG_MAX_BYTES` 32 Mo, `RUN_STOP_GRACE_MS`, `MPI_BIN` différé) ; `prisma/schema.prisma` modèle **`Run`** + migration `add_run_model` (statut/solver en `String` façon `User.role`, index `[projectId,status]`) ; **`lib/streamRunner.ts`** (nouveau seam injectable `setStreamRunner` — `child_process.spawn` **non détaché**, stdout/stderr pipés vers `solver.log`, **never-throw** : ENOENT/timeout/exit≠0 → `StreamExit` structuré ; l'`execFile`/`maxBuffer 16 Mo` du `commandRunner` est **inadapté** à un run multi-heures) ; `lib/runStorage.ts` (`runs/<id>/solver.log` **sibling de `case/`** → un reset de cas n'efface pas l'historique ; `readRunLog` par byte-offset) ; `lib/residualParser.ts` (parse `Time =` + `Solving for X, Initial residual = …` → `ResidualSample[]` ; détecte `converged`/`diverged` (nan-inf)/`foamError` ; `downsampleResiduals` borne la série) ; `modules/projects/runs.{service,controller,schemas}.ts` (`startRun` : assert visibilité → guard **409 `RUN_IN_PROGRESS`** → gate **409 `NO_MESH`**/**422 `NOT_RUNNABLE`** → solveur lu sur `controlDict.application` (borné `SOLVER_IDS`) → ensure `runTimeModifiable` → spawn → `running` ; `finalizeRun` classe le terminal sur exit + tail du log (converged/completed/diverged/failed/stopped) via `updateMany` filtré actif = premier writer gagne ; `stopRun` gracieux `stopAt writeNow` + SIGTERM après grâce, idempotent ; `getRunLog` catch-up { run, série, logTail, logBytes } ; **`reconcileOrphanRuns`** au boot → tout run actif orphelin = `failed`) ; `server.ts` (réconciliation avant `listen`) ; routes `GET/POST /:id/runs`, `GET /:id/runs/:runId(/log)`, `POST /:id/runs/:runId/stop` ; `tests/helpers.ts` (`run.deleteMany`). **Décision** : updates live = **polling client** du `getRunLog` (réutilise l'auth en mémoire + refresh ; SSE/NDJSON différé). **Tests** : +11 API (`solver` : runner factice scripté — lifecycle converged/completed/failed/diverged, spawnError ENOENT, stop, guard concurrence, NO_MESH/NOT_RUNNABLE avant spawn, 404 étranger + super-admin, réconciliation orphelin) + 8 unité (`residualParser`). **Gates verts** : API typecheck + **203 tests** (+28 Slice 0+1), lint 0 erreur. **Note** : binaire solveur absent sur poste Windows → run `failed` propre (même classe que `pvpython`/`python3`). **Différé** : streaming temps réel, parallèle MPI (`decomposePar`/`mpirun`/`reconstructPar`), runs survivant au redémarrage API. |
| 2026-06-24 | Feature (Solveur — onglet « Solver » : moniteur de convergence live) | **Séquence skills `CLAUDE.md §0`** suivie : `ui-ux-pro-max` (cadrage charts/états) → `design-taste-frontend` (*design read* = **UI produit/outil, hors périmètre landing** → règles universelles : zéro em-dash, 1 accent orange, états complets, pas de slop) → `web-design-guidelines` (review : 2 correctifs appliqués — `History` passé `h2` (hiérarchie), `translate="no"` sur les noms de champs du graphe). **Web** : `lib/api/{types,projects}.ts` (types `RunSummary`/`RunnableCheck`/`RunLogPayload`/`ResidualSample` + `getRunnable`/`scaffoldSolver`/`startRun`/`listRuns`/`getRunLog`/`stopRun`) ; `features/solver/` (nouveau) — `useRuns.ts` (hooks React Query, **polling `refetchInterval`** tant qu'un run est actif, arrêt dès terminal), `runStatusMeta.ts`+`RunStatusBadge.tsx` (statut = **couleur + icône + mot**, vert `--color-success` pour *converged*, orange `accent`/texte `cta` pour *completed/diverged*, danger pour *failed*), `ResidualChart.tsx` (**SVG log-scale fait main**, 1 polyline/champ couleurs **token marque** + label en bout + légende + **table alternative** lecteur d'écran, `role=img`+`aria-label`, gridlines discrètes, état vide), `RunLog.tsx` (région `role=log` focusable `overscroll-contain`, auto-scroll au tail sauf si remonté), `RunHistory.tsx` (badges + durée/exit `tabular-nums`, `Intl.DateTimeFormat`), `SolverTab.tsx` (orchestrateur : skeleton / erreur+Retry / **gate non-runnable → CTA « Make runnable for simpleFoam »** + liste des fichiers manquants / config lecture seule (solveur, nProcs *Deferred*) + **1 seul CTA orange `Run`** / zone live = badge `role=status aria-live=polite` + chrono **hors** live-region (anti-spam) + itération + chart + log + **`Stop run` danger-outline** + bannières terminales `role=status`/`alert` / historique) ; `pages/ProjectDetailPage.tsx` (3e onglet **Solver** lazy code-split, gate `hasPolyMesh` + tooltip comme Visualize, monté **uniquement à l'ouverture**). **Conformité charte** : tokens uniquement, 1 orange/zone (Run ; Stop = outline danger), AA (`--color-cta` pour texte orange), états loading/empty/error/disabled, focus visibles, lucide (1 famille), zéro em-dash. **Tests** : +5 web (`SolverTab` 3 : gate+scaffold / Run CTA + historique vide / run converged → badges + bannière + chart ; `ProjectDetailPage` 2 : onglet Solver gaté sans mesh, ouvert avec). **Gates verts** : `build:shared`, web typecheck, **lint 0 erreur (aucune nouvelle warning)**, **87 tests web** (+5), build (chunk **`SolverTab` lazy ~17 ko / 5,4 ko gzip**). **Différé** : streaming temps réel (polling pour v1), graphe sans zoom/tooltip ni ligne de tolérance, sélection nProcs (parallèle différé), coloration 3D par champ (pression/vitesse) à l'arrivée des résultats. |

---

## 11. Backlog d'améliorations (analyse 5 agents — post-fondation)

> Issu de l'analyse de 5 agents le 2026-06-19 (la fondation `PLAN.md` §1-§9 étant livrée).
> L'utilisateur a choisi de construire **track 1 (Compte self-service)** en premier ; le reste est **validé mais pas encore construit**.
> ⚠️ Toute UI ici passe d'abord par la séquence skills obligatoire (`CLAUDE.md §0`). Vérifier que les références fichier/ligne tiennent toujours avant d'agir.

### Fait
| # | Amélioration | Détail |
|---|---|---|
| ✅ 1 | **Compte self-service** | Page `/account` + `PATCH /auth/me` + `POST /auth/change-password`. Voir §10 (2026-06-19). |
| ✅ 2 | **Révocation de session au changement mdp/rôle** | `tokenVersion` bumpé dans `users.service.updateUser` + au change-password. Voir §10. |
| ✅ 3 | **Correctifs & polish** | Bug AA sonner → `--color-cta` ; bordures d'erreur login ; `apps/web/.env.example` + garde `VITE_API_URL` ; durcissement API (`trust proxy`, `json limit 16kb`, secrets prod imposés). Voir §10 (2026-06-22). |
| ✅ 4 | **Robustesse fondation** | Pipeline CI GitHub Actions + table `AuditLog` (enregistrement admin/auth) + `GET /audit-logs` + page `/activity`. Voir §10 (2026-06-22). |
| ✅ 5 | **Profondeur back-office** | Recherche + tri + colonne `lastLoginAt` + statut Active/Disabled + désactiver/réactiver (`isActive`, login bloqué 403 `ACCOUNT_DISABLED`). Voir §10 (2026-06-22). |
| ✅ 6 | **Contrat partagé** | `packages/shared` (`@dive/shared`) : `Role`/`roleSchema`/longueurs/error codes consommés des deux côtés. Voir §10 (2026-06-22). |
| ✅ 7 | **Garde de saisie non sauvegardée** | `UnsavedChangesPrompt` (`useBlocker` + `beforeunload`) sur `/account` et le dialogue admin. Voir §10 (2026-06-22). |

### À faire (les « modifs manquantes » restantes)

**Track — Socle solveur + Home** *(le plus gros pas produit, ~L ; volontairement laissé pour une phase dédiée)*
- [x] **Import de cas OpenFOAM dans un projet** (dossier polyMesh / zip), arbre, download, **verify + scaffold** des fichiers de base manquants (`system/` trio + `0/{U,p}` génériques minimaux, `boundaryField` câblé sur les patches du mesh). 100 % applicatif. Voir §10 (2026-06-22, Feature import de cas).
- [ ] Modèles Prisma `Case` (projet CFD) + `Run` (statut, stub) ; routes `/cases` ; page liste + détail.
- [ ] Remplacer le `EmptyState` vide de la Home par une vraie page d'accueil « workspace ».
- [ ] Données seed réalistes (étage de turbine, etc.). Dispatch = stub tant que le serveur openFOAM n'existe pas.
- [~] **Conversion réelle du maillage** — **chemin CGNS livré 2026-06-23** : `pvpython CgnsToVtk.py` → `vtkUnstructuredToFoam` → `checkMesh`, piloté par « Convert to Foam » (toolchain Debian configurable, runner injectable, court-circuit + logs ; voir §10). **Restant** : variante Ansys/Fluent `.msh` → `constant/polyMesh/` via `fluent3DMeshToFoam -scale` ; `constant/...Properties` selon le solveur ; exécution **en tâche de fond** (modèle `Run`) plutôt que POST synchrone, quand le serveur distant existera. Règle à respecter : `0/*.boundaryField` doit couvrir **tous** les patches de `constant/polyMesh/boundary`.

---

#### Correctif — `CgnsToVtk.py` SIGSEGV headless (2026-06-24)
`pvpython CgnsToVtk.py` plantait (`SIGSEGV` dans `vtkSMParaViewPipelineControllerWithRendering::New()`) sur serveur sans display/OpenGL : `paraview.simple.SaveData` instancie un contrôleur **de rendu** qui crash en headless. Réécrit sans `paraview.simple` : lecture/merge/écriture via `vtkmodules` (`vtkCGNSReader` → `vtkAppendFilter` (MergePoints) → `vtkUnstructuredGridWriter` ASCII). Plus de server-manager ni de rendu = plus de segfault. Même contrat OK:/KO: et même sortie .vtk legacy ASCII pour `vtkUnstructuredToFoam`/meshio.

#### Correctif — chaîne CGNS→Foam opérationnelle (2026-06-25)
Trois bugs bloquaient la conversion réelle, corrigés et **validés sur le serveur** : (1) `CgnsToVtk.py` ne plantait plus en SIGSEGV mais échouait avec « File name not set » → exécution sous **`python3` + wheel `vtk`** au lieu de `pvpython` (mélange de deux builds VTK 9.6.2/ParaView 5.11.2 ⇒ `SetFileName` no-op + corruption mémoire) ; nouvelle var `CGNS_PYTHON_BIN` (défaut `python3`). (2) sortie VTK épinglée au format **legacy 4.2** + arrays de données strippées, sinon `vtkUnstructuredToFoam` (parser legacy) refusait le format 5.1. (3) garde anti-segfault + auto-diagnostic dans le script.

#### Feature — Visualize : édition groupée des patches + backup/restore du mesh (2026-06-25)
Onglet Visualize : l'édition inline (crayon + select par ligne) est remplacée par un **overlay unique** « Edit names & types » (`EditPatchesDialog`) qui édite tous les noms+types d'un coup avec validation (mot OpenFOAM valide + unicité) ; backend `editMeshPatches` (PUT `/mesh/patches`) applique le lot sur `boundary` + champs `0/` en un passage, renames sans collision (placeholders two-pass). **Backup à slot unique** : `meshBackupStorage` (copie de tout `case/` sous `projects/<id>/backups/`), capture auto de l'original à la 1ʳᵉ modif (`ensureOriginalBackup`, aussi en tête d'autoPatch), bouton « Overwrite backup » et « Restore » (confirm destructif) ; la restauration remet mesh **et** champs `0/`. Routes GET/POST `/mesh/backup` + POST `/mesh/backup/restore`. Skills CLAUDE.md §0 consultés (ui-ux-pro-max, design-taste-frontend ; `frontend-design` non installé). Tests : 230 API + 86 web verts.

#### Feature — Onglet « Export » : OpenFOAM → CGNS pour CFD-Post (2026-06-25)
Nouvel onglet **Export** (détail projet, gated sur la présence d'un mesh) qui convertit un cas OpenFOAM **résolu** en **CGNS** lisible par Ansys CFD-Post, **sans Fluent**. Pipeline backend 4 étapes (mirroir de la conversion CGNS→Foam : runner injectable, binaires configurables, rapport par étape) : **inspect** (profil du cas depuis les fichiers + `checkMesh` pour les polyèdres ; gate « cas résolu »), **convert** (`pvbatch FoamToCgns.py` → `export/out.cgns`, ADF, cell-centered — l'écriture CGNS est **ParaView-only**, core VTK n'a pas de writer), **validate** (relecture du CGNS via le wheel `vtk` sous `python3` : champs présents, cell-centered, zones non vides ; |U|max informatif), **cfdpost** (génère `session.cse` + mémo `LOAD_CFDPOST.md` — CFD-Post n'est pas sur le serveur). Artefacts sous `projects/<id>/export/` (sibling de `case/`). Routes POST/GET `/export` + download `/export/download/:artifact`. UI : bouton unique, rapport par étape (statut + logs repliables, état `warning`), profil, table de validation, téléchargements, mémo (pression ×ρ si incompressible, surfaces vides, transitoire). Nouvelles vars env : `PVBATCH_BIN`, `FOAM_DICTIONARY_BIN`, `POST_PROCESS_BIN`, `FOAM_TO_CGNS_SCRIPT`, `CGNS_INSPECT_SCRIPT`. Skills CLAUDE.md consultés (ui-ux-pro-max, design-taste-frontend ; review charte/AA appliquée). **Dev sans tester les vrais outils** (OpenFOAM/ParaView sur le serveur) : 246 tests API + 85 web verts, build prod OK ; scripts auto-diagnostiques (OK:/KO:) pour itérer sur le serveur. Risque connu signalé : `SaveData` CGNS peut segfault en headless si ParaView sans OSMesa (d'où `--force-offscreen-rendering`).
