# Base URL of the DIVE Turbinen API, including the /api/v1 prefix.
# Copy this file to apps/web/.env and adjust if the API runs elsewhere.
# Required: the web client fails fast on the first request if it is missing.
VITE_API_URL=http://localhost:4000/api/v1
